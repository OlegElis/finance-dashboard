# Finance Dashboard — правила проекта

## Общие правила

- **Автопуш**: всегда пушить в GitHub автоматически после каждого коммита, без вопросов
- **Фильтры per-tab**: каждая вкладка имеет свои фильтры. Изменение фильтров на одной вкладке НЕ влияет на другие
- **Изоляция вкладок**: при работе с одной вкладкой НЕ трогать код других вкладок

---

## Вкладка «Банк и касса»

### Артефакты

#### 1. Фильтр-бар
- Поля дат слева: `дд.мм.гггг — дд.мм.гггг`
- Пресеты справа: Сегодня, Вчера, Эта неделя, Этот месяц, Прош. месяц, Этот квартал, Прош. квартал, Этот год, Прош. год, Всё время

#### 2. KPI-бар
4 карточки: **Поступления / Списания / Сальдо / Счетов**
- Компактный (padding 5px, шрифт 14px), без подписи «руб.»

#### 3. Свод по движению денежных средств
- **Денежные средства** — открыта по умолчанию → подгруппы ООО, ИП (закрыты)
- **До налогового оформления** — открыта по умолчанию → подгруппы Карты ФЛ, Кассы ЦФО (закрыты)
- Треугольник: ▶ закрыто, ▼ открыто
- Клик на счёт → фильтрует таблицу Операции

#### 4. Графики
- **Верхний**: по умолчанию — месяцы текущего года; кнопки «По годам» / «По месяцам»
- **Нижний**: движение по дням; клик на день → фильтрует таблицу Операции

#### 5. Таблица Операции
- 22 строки видимо (445px), пагинация по 500
- Кнопка `× сброс` — сброс фильтра по счёту/дню
- Клик на строку → **Карточка операции** (всплывающий модал, все 15 полей)

**Колонки (порядок строгий):**
| # | Колонка | Выравнивание |
|---|---------|-------------|
| 1 | ДДС | центр, формат дд.мм.гггг |
| 2 | ОПиУ | центр, формат «Май 2026» |
| 3 | Тип операции | центр |
| 4 | ID Offer | левый |
| 5 | Поступление | правый |
| 6 | Списание | правый |
| 7 | ФНС | центр |
| 8 | Реквизиты счёта | левый |
| 9 | Статья | левый |
| 10 | Подстатья | левый |
| 11 | Аналитика | центр |
| 12 | Контрагент | левый |
| 13 | Назначение | левый |
| 14 | Направление | левый |
| 15 | Фин. операция | левый |

### Защищённые функции (не трогать при работе с другими вкладками)
`renderBank`, `bankFillTxn`, `bankShowCard`, `bankFilterByAccount`, `bankFilterByGroup`, `bankApplyFilter`, `bankRebuildChartData`, `bankUpdateDays`, `bankChartSwitch`, `bankApplyTxnFilter`, `bankTxnFilterByDay`, `bankTxnFilterByAcc`, `bankLoadMoreTxn`, `toggleBankSection`, `toggleSubGroup`

---

## Структура данных (COL)
```js
COL = {
  DATE_DDS: 0, DATE_OPIU: 1, TYPE: 2, OFFER_ID: 3,
  INCOME: 4, OUTCOME: 5, FNS: 6, ACCOUNT: 7,
  ARTICLE_CODE: 8, ARTICLE_NAME: 9, ANALYTICS: 10,
  SECOND_SIDE: 11, PURPOSE: 12, ACTIVITY_DIR: 13, FIN_OP: 14
}
```

## API
- Google Sheets API, лист `RawData`, первая строка — заголовки
- Запрос: `valueRenderOption=UNFORMATTED_VALUE&dateTimeRenderOption=SERIAL_NUMBER`
- Даты приходят как Excel-серийные числа → `parseDate` конвертирует
