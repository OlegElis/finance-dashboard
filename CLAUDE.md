# Finance Dashboard — правила проекта

> Дашборд управленческого учёта для ГК ОПОРА. Один файл `index.html` (весь HTML/CSS/JS,
> без фреймворков и сборщиков), хостинг GitHub Pages. Данные — Google Sheets API (read-only,
> API-ключ), лист `RawData`. Устройство вкладок, имена функций, поля состояния и структуру
> данных смотри в коде — здесь только то, чего в коде нет: договорённости, запреты,
> нестандартные решения и причины их принятия.
> История и обоснования решений - docs/DECISIONS.md. Перед правкой места, о котором есть пункт, прочитать его полную версию там.

## Общие правила

- **ПОРЯДОК РАБОТЫ: пуш ПОСЛЕ проверки на живых данных, а не до.** Пять шагов: правка → стенд → КОММИТ → проверка на боевых числах (Олег) → ПУШ по отмашке. ⚠ Стенд боевых данных не заменяет: ловит синтаксис, логику, вёрстку. ⚠ Пуш до шага 4 недопустим; порядок один на всё. ⚠ Шаг 4 обязателен, если тронут `index.html`; только документы - пуш сразу после коммита. ⚠ Оговорка смотрит на ВЕТКУ, а не на коммит: с непроверенным `index.html` документный коммит ждёт общей отмашки. Подробно: docs/DECISIONS.md#d-push-after-live-check
- **ПЕРЕД КАЖДЫМ ПУШЕМ ТЕРМИНАЛ ПЕРЕЧИСЛЯЕТ, ЧТО УЙДЁТ** (правило Олега 07.09.2026): `git log origin/main..HEAD --oneline` ДО `git push`, пуш после сверки списка. ⚠ `git push` отправляет ВСЮ ветку, а не последнюю задачу. ⚠ Есть НЕПРОВЕРЕННОЕ (`index.html` без шага 4) - сказать ДО пуша. ⚠ Печатать ИМЕННО список коммитов, не «есть изменения». Подробно: docs/DECISIONS.md#d-list-commits-before-push
- **Сравнение разметки байт в байт: условные куски шаблона тащат за собой ПЕРЕНОС СТРОКИ.** `${cond?`<th…>`:''}` на отдельной строке при `cond=false` оставляет перевод строки. Условный кусок дописывать НА ТОЙ ЖЕ строке, что и соседний тег; сравнивать по `.outerHTML` таблицы, а не по всей вкладке. Подробно: docs/DECISIONS.md#d-template-newline-diff
- **Дефис, не тире**: нигде не использовать длинное тире, только «-» (код, UI, чат). ⚠ Правило про ТИПОГРАФИКУ, а не про каждый символ в файле: в трёх местах тире это ДАННЫЕ, слепая замена ломает их МОЛЧА - `EMP`/`EMPTY` перечисляют тире как значения «пусто», `artLabel` держит их в регулярке. Перед чисткой смотреть, текст это или разбор. Подробно: docs/DECISIONS.md#d-hyphen-not-dash
- **Фильтры per-tab**: у каждой вкладки свои фильтры (`_tabFilters[tab]`). Изменение фильтров на одной вкладке НЕ влияет на другие.
- **Изоляция вкладок**: работая с одной вкладкой, НЕ трогать код других. У каждой вкладки есть набор «защищённых» функций (`renderBank`, `bankFillTxn`, `renderBflModel` и т.д.) — не менять их при работе над чужой вкладкой.
- **Как поднимается локальная проверка** (шаг 4 порядка выше): `python3 -m http.server 8000` в папке проекта, открывать `http://localhost:8000`. ⚠ Через `file://` дашборд не работает - Firebase Auth живёт только на http/https, данные не загрузятся вовсе. В Google Cloud Console → API-ключ → HTTP referrers должен быть разрешён `http://localhost:8000/*` (уже добавлен).
- **Снимок для консоли обязан начинаться с преамбулы**: переход на вкладку, ожидание её узла, страж на данные - рендереры выходят без контейнера (`registryRender`), с чужой вкладки снимок отдаёт нули. ⚠ `RAW` объявлена `let RAW=[]`, на `window` её НЕТ: проверять `typeof RAW==='undefined'||!RAW.length`, а не `!window.RAW`. В конце вернуть состояние; прогонять со старта с ЧУЖОЙ вкладки. Подробно: docs/DECISIONS.md#d-console-snippet-preamble

## Данные и источник

- Google Sheets API, `valueRenderOption=UNFORMATTED_VALUE&dateTimeRenderOption=SERIAL_NUMBER`. **Даты приходят Excel-серийными числами** → `parseDate` конвертирует. Первая строка `RawData` — заголовки; индексы колонок фиксированы (`COL` в коде), перестановка колонок молча ломает отчёты.
- **Данные собираются через IMPORTRANGE** из источников. Во время пересчёта импорта шапка временно приходит пустой / `#ERROR!` / `Loading…` — поэтому сверка структуры листа (`connectAndLoad`, эталон в localStorage `fin_rawHeaders_{sheet}`) идёт ТОЛЬКО по «полной валидной» шапке (15 непустых, без `#`/`loading`), иначе была бы ложная тревога.
- **Медленное открытие - это ПЕРЕСЧЁТ формул, а не объём данных** (замер 30.07.2026). Лечится заливкой листа значениями - `apps-script/freeze-rawdata.gs` (формулы на `RawData_src`). ⚠ Кэш RAW в IndexedDB НЕ делаем. ⚠ Загрузка по периоду скорость НЕ чинит - не возвращать как «оптимизацию»; вернуться, когда открытие перевалит за 8-10 с. Справочники (`loadBflDeals`/`loadOfferRef`) - через `Promise.all`. Подробно: docs/DECISIONS.md#d-slow-open-formula-recalc
- **Кэша RAW между открытиями НЕТ на пути Sheets API** (решение 30.07.2026, в силе): дисковый кэш - чужие финансовые данные на диске (см. `appLogout`). ⚠ Оговорка с 15.08.2026: у пути СНИМКА кэш есть - не отмена решения: сжатые байты, стирается в `appLogout`, чужой кэш отсекается по `email + _dataSheet`; инвалидации «по возрасту» нет. Подробно: docs/DECISIONS.md#d-no-raw-cache-sheets-path
- **Быстрый путь данных: СНИМОК в RTDB** (15.08.2026, `apps-script/snapshot-rtdb.gs`, `config/dataSource`). ⚠ У подписки ТРИ случая, третий - нет узла. ⚠ `null` ≠ `'sheets'`. ⚠ Путь Sheets - точка отката, не удалять. ⚠ Гейт по листу `_snapEnabled`. ⚠ Сбой = откат на Sheets, бейдж «⚠ Sheets - запасной путь». ⚠ `_snapJoin` - числовой порядок ключей. ⚠ Подписка на `meta`. ⚠ `MIN_PUBLISH_MIN=30` не трогать. ⚠ Трафик = число публикаций. ⚠ Σ меток `showStats()` = вызовы `run_`. ⚠ Правая часть - признак, не список. ⚠ Новая точка входа - в `RUN_ENTRIES`. ⚠ Не фильтровать по одному `publishSnapshot`. ⚠ На 23.08.2026 - 96. ⚠ `KEEP_GENS=2`, `prune_` не бросает. ⚠ Шестой метки нет. ⚠ `lastPruneError`/`lastPruneOk` не стирать. ⚠ Тревоги `pruneCheck_` одним письмом. ⚠ Порог > `KEEP_GENS+1`. ⚠ Расписание - `trigSpec`. ⚠ `snapshotGens()` только shallow. ⚠ Кэш - IndexedDB, gzip. ⚠ Обновление ждёт `_uiBusy`/`_whenUiFree`. ⚠ Приёмка `_cmpSources()`. Подробно: docs/DECISIONS.md#d-snapshot-rtdb
- **Чистка истории посещений (`activity/*`): ретенция 90 суток, чистит СКРИПТ** (`pruneActivity_`, с 24.08.2026); 90 заново не обсуждается. ⚠ В браузер не переносить: клиент, чужие узлы, расписание у скрипта. ⚠ Две операции на одном триггере НЕ СВЯЗАНЫ: свои `try/catch`, `lastActPruneError`/`lastActPruneOk`, общий `pruneStateLine_`. ⚠ Шестой метки нет. ⚠ Зовётся из `finally` у `publishSnapshotDaily`, не из `run_`. ⚠ Удаляется узел ДНЯ целиком, `lastSeen` не трогается, не-дни не удаляются. ⚠ Граница СТРОГАЯ: день ровно 90 суток назад остаётся. ⚠ Сторож `activityCheck_` перечитывает базу, остаток = отказ; `showActivity()`, `pruneActivityNow()`. ⚠ Тревоги одним письмом (`alertPush_`). ⚠ Подпись в `showEmployeeActivity` называет срок (`ACT_WINDOW_DAYS`). ⚠ ПОРЯДОК ЖЁСТКИЙ (24.08.2026): подпись раньше чистки, `installTriggers()` до неё не запускать. ⚠ `ACT_KEEP_DAYS` и `ACT_WINDOW_DAYS` менять одной правкой. Подробно: docs/DECISIONS.md#d-activity-prune-90-days
- Архивирование: данные тянутся через отдельный Apps Script (замороженный XLSX у источника); настройки бэкапятся кнопкой в дашборде (общее меню: `downloadBackup` - данные+config целиком).
- **Архив настроек БДР**: отдельно от `downloadBackup`; `config/*` целиком по чёрному списку `BACKUP_EXCLUDE`. Восстановление `pfBackupRestoreOpen`: превью, слово `ВОССТАНОВИТЬ`, авто-скачивание текущего, запись через `_fbSetSafe`. `pfBackupPrompt` - перед заморозкой и `planFillAllFact`, на `planSave` НЕ вешать. `config/backupLog` - бейдж давности. Подробно: docs/DECISIONS.md#d-config-backup-archive
- **Ручные количества маркетинга - в облаке** (`config/mktCounts`, с 02.08.2026), `{направление:{период:{…}}}`, зеркало в `localStorage`. ⚠ Верхний уровень кодируется `_rtdbEncObj`, вложенный - нет. ⚠ `startMktCountsSync` перерисовывает ТОЛЬКО при реальном изменении (сравнение JSON). ⚠ Миграции «локальное → облако» СОЗНАТЕЛЬНО нет. ⚠ Запись в `config/*` только админу - у сотрудника отказ `_fbSetSafe`. Подробно: docs/DECISIONS.md#d-mkt-counts-cloud
- **Листы-справочники**: `RawData_BFL` - только БФЛ; `RawData_ID_Offer` - сделки ВСЕХ направлений. Матчить по системному ID (число до «|»), не по `ID_Offer`/ФИО/№ договора; нормализация `_arNormId`. ⚠ `RawData_ID_Offer` приезжает из Битрикса24, читается через Sheets API (`loadOfferRef`), через снимок НЕ идёт - сторожа снимка его не покрывают; см. `handoff.md`, 16.08. Подробно: docs/DECISIONS.md#d-reference-sheets-match-by-id
- ⚠ **Количества по НАПРАВЛЕНИЯМ из справочников не достаются, и это вопрос методики, а не данных** (31.07.2026, закрыто): «Направление маркетинга» - канал привлечения (`bflMapDir`); связка по `ID_Offer` неоднозначна - нужно правило разнесения из учёта. Авто из `RawData_BFL` только для БФЛ, прочие - ручной ввод (`_mktCounts`). Подробно: docs/DECISIONS.md#d-no-direction-counts

## Дизайн-система

- Все цвета/шрифты — CSS-токенами в `:root`, **не хардкодить hex** в компонентах (точные значения — в коде). Стиль: спокойный, плотный, данные-первичны.
- **Цвет = знак значения**: доход/прибыль зелёный (`--income`), расход/убыток красный (`--outcome`), остаток ДС оранжевый. Заголовки секций — нейтральный `--text2`, НЕ акцент.
- Бренд-акцент — графитово-синий `--accent` (был фиолетовый `#7c4ddb`, заменён по просьбе). Только для активной навигации/чекбоксов/логотипа/линий графиков, не для декора.
- Шрифты: IBM Plex Sans (интерфейс) + IBM Plex Mono с `tabular-nums` для ВСЕХ чисел. В `.report-table` 1-я колонка — текстовый лейбл (sans), остальные — числа (mono).
- Chart.defaults — mono, оси серые. Маркетинговая палитра завязана на легенды-чипы — не трогать без синхронизации легенды и графика.
- **Математический ноль гасится в ФОРМАТТЕРАХ, а не в расчётах** (`_z0`, порог 0.005 = полкопейки): иначе вывод «-0,00». `_z0` стоит в общей `fmt`, в `numClass` и в локальных форматтерах денег (`f2`, `fmt2`, `fmtR`, `money`), которые общую `fmt` не зовут. Суммы остаются точными. Подробно: docs/DECISIONS.md#d-zero-in-formatters
- **РУБЛИ СЧИТАЮТСЯ ИЗ РУБЛЕЙ, ПРОЦЕНТЫ - ТОЛЬКО ДЛЯ ПОКАЗА; ОКРУГЛЕНИЕ ВСЕГДА ПОСЛЕДНЯЯ ОПЕРАЦИЯ** (27.08.2026). ⚠ Показанная разность A и B обязана равняться разности показанных. ⚠ Ошибка МОЛЧАЛИВАЯ: пересчёт из округлённого процента правдоподобен. ⚠ Первой споткнулась «Динамика сбора внутри месяца». ⚠ Округление для показа - в форматтере, обратно не читать. ⚠ Медиана и агрегаты по долям - от ТОЧНЫХ долей (`rub/plan`). Подробно: docs/DECISIONS.md#d-rubles-from-rubles

## Фильтр-бар вкладки: одна строка, один источник габаритов (07.09.2026)

- **ЭТАЛОН - БАР ВКЛАДКИ БАНК**, единственный источник габаритов - класс **`.bar-ctl`** (высота 21px), полоса `min-height:27px`, зазор 10px между группами, 6px между контролами. ⚠ Заведён потому, что инлайновые габариты в рендерерах разъехались. Новый контрол берёт класс, а не копию стиля - держит тест. Подробно: docs/DECISIONS.md#d-bar-ctl-single-source
- **ВСЕ СЕМЬ БАРОВ - В ОДНУ СТРОКУ ПРИ 1600** (Банк, Реестр, БДР, ОПиУ, ДДС, ОСВ, ФОТ), высота полосы 28px. При 1200 и 900 перенос допустим - как у Банка.
- **ПОДПИСЬ-LABEL РЯДОМ С КОНТРОЛОМ НЕ СТАВИМ.** Категория живёт ВНУТРИ контрола («Статьи: все ▾», «Период: по месяцам»). ⚠ Подпись кнопки строит ОДИН источник `_barBtnLabel(категория, n)`, его же зовут обработчики обновления. ⚠ Категорию не убирать даже там, где «и так понятно» (Год и Месяц). Подробно: docs/DECISIONS.md#d-category-inside-control
- **РЕДКИЕ КОНТРОЛЫ - ПОД «⋯», НО НЕ УДАЛЕНЫ** (`_barMore`/`_barMoreRow`/`_barMoreToggle`; **БДР (с 13.09.2026) - троица «🔒 статус утверждения · 💾 архив · ⚙ Действия»**, `_pfMoreRows`); меняется только место показа. ⚠ Кнопка помечается «⋯ •», когда внутри задано, и называет это в подсказке. ⚠ У БДР под панелью СИГНАЛЫ («⚠ 0/12», архив, проверка) - точка по ним (`_pfMoreSig`). ⚠ «Сбросить (N)», «Пресет» и «Разрез» под «⋯» НЕ уходят (решение Олега 13.09.2026). ⚠ Редкий фильтр не удалять ради места. Подробно: docs/DECISIONS.md#d-rare-controls-under-more
- **ВСПЛЫВАЮЩАЯ ПАНЕЛЬ БАРА: ТРИ ПРАВИЛА** (08.09.2026). (1) Закрытие по клику ВНЕ - только `mousedown` (`BAR_MORE_OUT_EVT`), иначе у `select` не долетает `change`. (2) Состояние «открыта» - только `window._barMoreOpen`, восстанавливается при сборке разметки (`_barMore`). (3) Контрол панели, меняющий данные, переживает ре-рендер - структурный тест `pfRunTests()`. Подробно: docs/DECISIONS.md#d-bar-popup-three-rules
- **ТУМБЛЕР ВНУТРИ ПАНЕЛИ «⋯» СОБИРАЕТСЯ ТОЛЬКО ЧЕРЕЗ `_barMoreCtlToggle`, ПОДПИСЬ - «вкл»/«выкл» ПО СОСТОЯНИЮ** (08.09.2026): жёсткое «вкл» говорит команду, а не статус. ⚠ Контролы в САМОМ баре под правило не подпадают - у них своя подпись и место в ряду (например «Будущие оплаты» на Банке). Подробно: docs/DECISIONS.md#d-more-panel-toggle-label
- **СВОДКА «Фильтры: …» ЖИВЁТ ВНУТРИ БАРА** (как `.bank-summary` у Банка): `margin-left:auto`, `flex:0 1 auto`, `min-width:160px`, ellipsis. Своей полосой она стоила 22px высоты на ДДС, ОПиУ, ОСВ и ФОТ. Порядок «сводка → селект пресета» держится сам: `_tabFilterSummary` всегда зовётся раньше `_presetBarInject`.
- **ВЫСОТА БАРА - ИНВАРИАНТ, СВОДКА - ЕДИНСТВЕННЫЙ РЕЗИНОВЫЙ ЭЛЕМЕНТ (принято 09.09.2026).** Не влезло - сжимается сводка (полный текст в `title`). ⚠ У сводки `flex:1 1 0`, базис РОВНО НОЛЬ, не `auto`. ⚠ У БДР и Реестра сводки нет - высота держится шириной контролов, новый контрол требует замера. ⚠ Банк при 1280: `min-width` кнопок по самой широкой подписи. ⚠ Селект пресета: `max-width` 104, имя в `title` (`_presetPickTitle`). ⚠ `.bank-extra-filters` переносит детей внутри. ⚠ `flex-wrap:wrap` ОСТАЁТСЯ страховкой на 900. ⚠ Держит `barRunTests()`: 28px при 1600 на семи вкладках, при 1280 на пяти. ⚠ При провале `_barDiag` печатает, что не влезло. Подробно: docs/DECISIONS.md#d-bar-height-invariant
- ⚠ **Вкладок БЕЗ пресетов правка не касается**: их поля дат и селекты держит прежнее правило через `:not(.bar-ctl)` - трогать их отдельным решением.
- **ПУСТАЯ ВЫБОРКА РИСУЕТ ТОТ ЖЕ БАР, ЧТО И НЕПУСТАЯ** (15.09.2026). Выход «нет данных» у ОПиУ - ПОСЛЕ сборки `extraFiltersOPIU`; списки от `baseData`/`RAW`, не от `data`. ⚠ Ранний выход на любой вкладке с баром отдаёт ПОЛНЫЙ бар. ⚠ `_barDiag` различает «НЕ ВЛЕЗЛО» и «НЕ НАЙДЕН»; `barRunTests` печатает число проверок против ожидаемого. ⚠ При запертой «Сбросить» тест печатает, ПОЧЕМУ. ⚠ `barRunTests` кладёт фильтры ПОВЕРХ текущего состояния вкладки. Подробно: docs/DECISIONS.md#d-empty-selection-full-bar
- **КНОПКА «СБРОСИТЬ (N)» - ОДНА НА СЕМЬ ВКЛАДОК, СТОИТ ПЕРЕД СЕЛЕКТОМ ПРЕСЕТА** (задача Олега 13.09.2026). Ставит `_presetBarInject`; счёт `_tabActiveFilters`, сброс `_tabResetFilters` → `_tabResetCore`; прежние сбросы удалены. ⚠ Кнопка есть ВСЕГДА, в чистом состоянии `disabled`. ⚠ N считается от умолчаний вкладки - один источник `_TAB_BLANK`/`_tabBlankState(tab)`. ⚠ `TAB_COUNT_SKIP` не считается, но сбрасывается; считается ровно то, что в сводке «Фильтры: …» (тест). ⚠ Сброс снимает пресет всегда. ⚠ Наблюдатель зовёт и `_tabFilterSummary`; порядок: сводка → «Сбросить (N)» → пресет. ⚠ `barRunTests` проверяет кнопку. Подробно: docs/DECISIONS.md#d-single-reset-button
- **ПЕРИМЕТР ОДИН НА ВСЕ СЕМЬ ВКЛАДОК И ЗАДАН ОДНИМ ПРАВИЛОМ** (07.09.2026): `.content:has(#tab…:not(.hidden)){padding:12px}`; зазор до содержимого 8px. ⚠ Общие `.content{padding:24px}` и `.kpi-grid{margin-top:6px}` НЕ трогаем. ⚠ Правило в `@media screen` - на печати прежние 24px. ⚠ Высота `#tabPlanfact` (`calc(100vh - 80px)`) завязана на паддинг - менять вместе. Подробно: docs/DECISIONS.md#d-single-content-padding-rule
- ⚠ **У БДР `margin-left:auto` несёт обёртка кнопки «⋯»** (с 13.09.2026): один элемент прижимается вправо целиком. Селект пресета добавляется ПОСЛЕ «⋯». Меню «⚙ Действия» открывается ВНУТРИ панели (`pf-actions-dd` якорится на обёртке); после ре-рендера панель возвращается открытой общим `_barMore`. Подробно: docs/DECISIONS.md#d-pf-more-wrap-margin-auto
- ⚠ **Правку бара мерить, а не смотреть**: высота бара при 1600 обязана быть 28px и одна строка. На ОСВ потерянные при переписывании четыре `</div>` дали бар в 328px, вобравший всю вкладку, - глазами в консоли это не видно, замером видно сразу.

## Подсказки (единый компонент)

- **Подсказка с ЧИСЛАМИ - только через единый компонент** (`_tipAttr` в разметку / `_tipSet` из JS → `_tipShow`, узел `#uiTip`). Нативный `title` числа не выравнивает (`padStart` столбик не держит). Выравнивание держит **сетка** (`uit-grid`, 2 или 3 колонки), числа моно с `tabular-nums`. Подробно: docs/DECISIONS.md#d-tip-component-numbers
- **Что переводить, а что нет**: на компонент - только подсказки с ЧИСЛАМИ или многострочным списком. Подсказки на контролах и пояснения к колонкам и режимам ОСТАЮТСЯ нативным `title`. «Операции: <период>», стадия сделки в Дебиторке, фильтр Аномалий, KPI «Исполнение YTD» - нативные СОЗНАТЕЛЬНО. Подробно: docs/DECISIONS.md#d-tip-what-to-migrate
- ⚠ **Узел живёт в `<body>` с `position:fixed`, позиция считается от `getBoundingClientRect()` якоря.** Внутри скроллящегося тела таблицы (`.table-scroll`, тела карточек Банка) подсказка обрезалась бы по `overflow` - ровно то, на чём горели выпадающие списки фильтров. У краёв окна разворачивается внутрь (вправо/вверх).
- Показ - ОДИН делегированный слушатель на `[data-tip]`: подсказки переживают любой ре-рендер. Гасится уходом курсора, Esc, кликом (capture), скроллом (capture - контейнеры до `window` не всплывают), `beforeprint` и `blur`. На тач - показ по тапу с автоскрытием 4 с. `@media print` гасит узел. Подробно: docs/DECISIONS.md#d-tip-delegated-listener
- `z-index:100003` - выше всех оверлеев проекта (потолок был 100002): подсказка обязана быть поверх модалов.
- ⚠ **Значения в `_tipAttr` передавать СЫРЫМИ** (`_htmlEsc` он делает сам). Предварительный `esc` даст двойной эскейп - «&amp;amp;» в именах.
- ⚠ Где подсказка меняется ПОСЛЕ рендера (редактор планов: «⤓ заполнить фактом», undo) - только `_tipSet`, и снимок undo хранит `data-tip`, а не `title`. Иначе поверх компонента ляжет нативный `title` и подсказка будет выглядеть по-разному до и после нажатия кнопки.
- **Графики (Chart.js) свой tooltip НЕ меняют на компонент** - там выравнивание уже держится (холст, моношрифт, `padStart` работает). К стандарту приведён только ВИД, и задан он ОДНИМ местом - `Chart.defaults.plugins.tooltip` рядом с `Chart.defaults.font`. По графикам не размазывать: иначе следующий график заведут со старым видом и никто не заметит.
- **Числа в подсказке - ПОЛНЫЕ, всегда.** Сокращения `М`/`К` остаются только на подписях ОСЕЙ и надписях на холсте (`moneyTick`, `km`, `abbr`, `kfmt`). ⚠ Где на одном графике деньги и количества (юнит-экономика) - целые без копеек, дробные через `fmt`, иначе у количеств появится «12,00». Подробно: docs/DECISIONS.md#d-tip-full-numbers
- ⚠ `displayColors:false` в подсказке дневного графика Банка - НЕ косметика: с цветными квадратами Chart.js сдвигает строки ТЕЛА вправо, а footer нет, и разряды разъезжаются.

## Безопасность (по аудиту)

- **`_htmlEsc(v)`** — ЕДИНСТВЕННЫЙ способ вставлять данные из Sheets (ячейки RAW) в `innerHTML`/`onclick`. Без него — хранимый XSS через ячейку. Применять во ВСЕХ новых рендерах для любых данных-лейблов.
- **CSV-экспорт** (`registryExportAI`): текст, начинающийся с `= + @ -` или таба, префиксовать апострофом (защита от формульных инъекций в Excel).
- **`_isAdmin()`** = текущий пользователь == `ADMIN_EMAIL` (`o017ev@gmail.com`). Гейтит запись общих `config/*` и админ-UI. Реквизиты `config/sheets` пишет ТОЛЬКО админ, сотрудники читают — иначе при правиле «запись = админ» каждый вход сотрудника ловил бы отказ. Попап лимитов реквизитов для не-админа — только просмотр.
- **ВИД НЕ-АДМИНА ПРОВЕРЯЕТСЯ ДО ВЫКЛАДКИ - подменой `ADMIN_EMAIL` в ЛОКАЛЬНОЙ копии** (26.08.2026): подменить константу в `index.html` → стенд (`python3 -m http.server 8000`) → войти СВОЕЙ учёткой → смотреть глазами → **вернуть файл** (`git checkout -- index.html`). Правка НЕ коммитится никогда. Подробно: docs/DECISIONS.md#d-nonadmin-view-local-swap
  - ⚠ **Подменять надо КОНСТАНТУ, а не `_isAdmin()`**: сравнение с `ADMIN_EMAIL` продублировано инлайном ещё в пяти местах (`renderPlanFact`, `showPresenceList`, `window._planReadOnly`, `onAuthStateChanged`, `resolveAccess`); правка одной функции дала бы полу-админский гибрид. Подробно: docs/DECISIONS.md#d-swap-constant-not-isadmin
  - ⚠ **Одной правки хватает не всегда: та же константа гейтит ДОСТУП, а не только роль.** `resolveAccess` сверяет с ней email ПЕРВЫМ; нет адреса в `config/allowedEmails`/`config/sheetAccess` - экран входа (`notlisted`). Тогда в `resolveAccess` оставить доступ (локальный `isAdmin` → `true`). Подробно: docs/DECISIONS.md#d-swap-gates-access-too
  - ⚠ **Приём проверяет ВИД, а не ПРАВА.** Firebase видит настоящий адрес, правила RTDB остаются админскими: незагейченная запись в `config/*` в этом прогоне ПРОЙДЁТ и уедет в облако. Отказ базы так не проверяется - нужна учётка сотрудника. Ловится «пункт показан не так», не «гейт записи потерян». Подробно: docs/DECISIONS.md#d-swap-checks-view-not-rights
  - ⚠ **Самое опасное - забыть вернуть файл.** `ADMIN_EMAIL` живёт в том же `index.html`, и уехавшая на Pages подмена снимает админские права на боевой странице - молча и у всех сразу. Возвращать СРАЗУ после просмотра, а не «потом»; перед коммитом и перед пушем `git status` обязан быть чистым.
  - Чем этот приём НЕ является: стенд сверяет РАЗМЕТКУ строкой (в том числе побайтное совпадение админского варианта - см. блок про меню «⚙ Действия»), подмена показывает ВИД глазами. Они не заменяют друг друга: первое ловит незамеченную правку чужого варианта, второе - то, что человек увидит на экране.
- ⚠ Остаётся по дизайну: **Sheets API-ключ в исходнике** (нужно ограничение по referrer в Google Cloud Console). Клиентская модель доступа — **барьер от посторонних, а не строгая изоляция** (продвинутый юзер может запросить чужую вкладку; строгий вариант — Apps Script-прокси).

## Надёжность (по аудиту)

- **`_fbSetSafe(path,val,label)`** — ЕДИНСТВЕННЫЙ способ писать в `config/*`. Раньше сейвы были fire-and-forget — «✓ Сохранено» врало, планы терялись при следующей загрузке.
- ⚠ **У отказа записи ТРИ разные механики, и сообщение обязано их различать** (31.07.2026): правила БД → промис ОТКЛОНЯЕТСЯ навсегда; офлайн → ВИСИТ, дойдёт при реконнекте; запрещённый символ в ключе → SDK БРОСАЕТ синхронно; оба пути через общий `fail()`. ⚠ Формулировка «сохранено только на этом устройстве» убрана СОЗНАТЕЛЬНО, не возвращать. Проверка отказа правил: `_fbSetSafe('config/__rule_probe',…)`. ⚠ НЕ снимать правило с рабочей ветки ради теста. Подробно: docs/DECISIONS.md#d-fbsetsafe-three-failures
- ⚠ **Инвариант зеркала: `localStorage` = ПОСЛЕДНЕЕ ИЗВЕСТНОЕ ОБЛАКО, а не «что я тут набрал».** `_plansSave`/`_baselineSave` пишут зеркало только при ПОДТВЕРЖДЁННОЙ записи (fail-open без облака, `_fbCloudOn()`). ⚠ Схему `savedAt` из модели БФЛ сюда переносить НЕЛЬЗЯ: `_bflModelSave` ставит метку ДО записи. ⚠ Инвариант НЕ глобальный: `_ownersSave`/`_notesSave`/`_presetsSave`/`_kassaSaveLimits` и прочие ветки пишут зеркало безусловно. Подробно: docs/DECISIONS.md#d-mirror-last-known-cloud
- **Гейт `window._planEditorOpen`** стоит на листенерах `plans`, `plansBaseline`, `planOwners`, `planNotes`: пока открыт редактор планов, приход облака не затирает несохранённые правки прямо под руками. Сохранять его при правке любого из четырёх листенеров.
- Каждый рендерер обёрнут в try/catch (`renderActiveTab`) — упавший показывает заглушку с текстом ошибки, а не мёртвую вкладку.
- **Logout обязан сбрасывать сессию**: `appLogout` гасит презенс и обнуляет `_appBooted`/`_dataSheet`, чтобы следующий вход перегрузил данные СВОЕЙ вкладки. Раньше новый пользователь видел RAW предыдущего. `watchForceLogout` сравнивает с `_flAuthMs` (обновляется на каждом входе), а не с `auth_time` первого входа.
- **MutationObserver-ловушка**: мобильные хелперы (`watchMobileCards`) и переинжект пресетов (`_setupPresetObserver`) слушают `#mainContent` и срабатывают на каждый ре-рендер. ⚠ Их колбэки НЕ должны безусловно мутировать DOM (писать `innerHTML` только при реальном изменении; сводку фильтров обновлять по data-sig) — иначе observer зацикливается.
- **330 тыс. узлов DOM при 18 отрисованных вкладках - ИЗВЕСТНОЕ состояние, сегодня НЕ проблема** (01.08.2026). Возвращаться сюда, если вкладок станет больше или появятся тяжёлые таблицы. ⚠ Очистка неактивных вкладок (`innerHTML=''` в `switchTab`) - только отдельной задачей: правит все вкладки, висячие холсты Chart.js, будит MutationObserver на `#mainContent`, теряет прокрутку и `<details>`. ⚠ Мерить `switchTab` через `performance.now()` вокруг САМОГО вызова, без `await` загрузки. Подробно: docs/DECISIONS.md#d-dom-nodes-known-state
- **Именные ловушки при правке кода**: `presetApply` - пользовательские пресеты (дропдаун), `setPreset` - пресеты периода; не путать (старые быстрые дат-чипы и функция `applyPreset` удалены). Глобального `esc` нет (он локален в каждой render-функции) - для эскейпа вне рендера свои хелперы (`_pesc`, `_htmlEsc`, `_dEsc`).
- **RTDB превращает числовые ключи в МАССИВ.** `config/plansBaseline/<year>/<month>` приходит объектом или массивом; читатели (`_baselineMonthsMap`) толерантны к обеим формам - `Object.keys()` работает везде, ранний выход по `Array.isArray()` теряет данные. При записи нормализовать к плоскому объекту. Подробно: docs/DECISIONS.md#d-rtdb-numeric-keys-array

## Пресеты отчётов

- **ПРЕСЕТ - ЗАДАННЫЙ НАБОР ПОЛЕЙ, А НЕ СЛЕПОК ЭКРАНА** (решение Олега 07.09.2026). ⚠ Снимок отменён: непрозрачен. ⚠ «НЕ ЗАДАНО» = УМОЛЧАНИЕ ВКЛАДКИ, А НЕ «ОСТАВИТЬ КАК ЕСТЬ»: применение = сброс к умолчаниям (`_presetTabReset` первой строкой `presetApply`), затем заданные поля; пустой пресет ЧИСТИТ вкладку. ⚠ Умолчание - у САМОЙ ВКЛАДКИ (`bankDefaultPeriod`, `_regBlankState`, `_presetTabDefaults`). ⚠ В `presetApply` - `let F`, не `const`. ⚠ «Сбросить (N)» показывает, сколько фильтров стоит СЕЙЧАС. Подробно: docs/DECISIONS.md#d-preset-fields-not-snapshot
- ⚠ **Мультивыборы на тысячи значений в форме НЕ набираются** (статьи, ЦФО, контрагенты, колонки Реестра) - тип поля `snap`: значение кладётся кнопкой **«Подставить текущий отбор»** и показывается СОСТАВОМ; на вкладке у него своя машинерия (`pfSideRender`). Остальные поля - настоящие контролы. Подробно: docs/DECISIONS.md#d-preset-snap-fields
- **«Подставить текущий отбор» заполняет форму и НЕ СОХРАНЯЕТ**. ⚠ Задаются ТОЛЬКО поля, отличные от умолчания вкладки. ⚠ МИГРАЦИЯ РАБОТАЕТ ПО ДРУГОМУ ПРАВИЛУ, И СВОДИТЬ ИХ НЕЛЬЗЯ: она переносит в «задано» ВСЁ из снимка, включая равное умолчанию; иначе пресеты сузились бы МОЛЧА. Подробно: docs/DECISIONS.md#d-preset-fill-vs-migration
- ⚠ **ГОД У БДР хранится как `'cur'` или число**: пресет, сделанный в 2026, в январе 2027 молча показывал бы прошлый год.
- **МЕСЯЦ У БДР В ПРЕСЕТЕ БЫВАЕТ ОТНОСИТЕЛЬНЫМ - `period:'cur'` (текущий) и `'prev'` (прошлый), ТОЙ ЖЕ МЕХАНИКОЙ, ЧТО ГОД** (задача Олега 14.09.2026); разрешается в `presetApply` от даты применения. ⚠ Реализация ОДНА - `_pfPresetRelResolve` (год, оба месяца, `yearIsCurrent`); тест держит ПОДМЕНОЙ функции. ⚠ «Прошлый» в январе - декабрь ПРЕДЫДУЩЕГО года и при абсолютном годе. ⚠ В баре вкладки вариантов НЕТ. ⚠ Миграции нет. Маркеры `PF_REL_CUR`/`PF_REL_PREV`. Подробно: docs/DECISIONS.md#d-preset-relative-month
- ⚠ **Период БДР**: пресет С периодом ставит `_defPeriodDone` (иначе умолчание «текущий месяц» тут же его снесёт), а пресет БЕЗ периода признак **не сбрасывает** - это тронуло бы поле, о котором он не говорит.
- ⚠ **ПОДПИСЬ ФИЛЬТРОВ (`_activePresetSig`) СНИМАЕТСЯ ДВАЖДЫ - ДО И ПОСЛЕ РЕНДЕРА, и оба раза нужны.** До - иначе `_presetActive` внутри рендера снимет только что применённый пресет. После - рендер дописывает состояние сам (у БДР умолчание периода). Симптом: отбор есть, в селекте «- не выбран -». Подробно: docs/DECISIONS.md#d-preset-sig-set-twice
- **ВЫБОР ПРЕСЕТА - НА ВКЛАДКЕ.** Селект - ПОСЛЕДНИЙ в баре после «Сбросить (N)», один. ⚠ Бар - место ВЫБОРА, не управления (оно в окне пресетов); пункт-действие и ⚙ убраны 07.09.2026. ⚠ Повод: иначе выбрать пресет нечем. ⚠ Отдельной полосы НЕ заводить. ⚠ Вставка ОДНА на семь вкладок, после рендера. ⚠ В бар добавляется РОВНО ОДИН элемент - селект; детей бара не трогать. ⚠ Прижатие вправо делает бар, не `margin-left:auto` у селекта. ⚠ Оформление - класс `select.preset-pick` (тест). ⚠ Правило - ПОСЛЕ `.dds-extra-filters select` и `.filter-extra select`. ⚠ `margin:-2px 0` не косметика. ⚠ `position:absolute` нельзя - ляжет на сводку. Подробно: docs/DECISIONS.md#d-preset-select-in-bar
- **ПОРЯДОК ПРЕСЕТОВ - ПОЛОЖЕНИЕ В МАССИВЕ `_presets[tab]`, ОТДЕЛЬНОГО ПОЛЯ СОРТИРОВКИ НЕТ; МЕНЯЕТСЯ ПЕРЕТАСКИВАНИЕМ ЗА РУЧКУ** (задача Олега 14.09.2026; кнопки ▲/▼ убраны). ⚠ Тащить ТОЛЬКО за ручку. ⚠ Pointer events (`_presetDragStart`/`_presetDragMove`/`_presetDragFinish`), не HTML5 DnD. ⚠ Запись в облако ОДИН РАЗ, по окончании: `_presetReorder` → `_presetsSave`. ⚠ Массив нигде не сортируется (`_presetsNormalize`). ⚠ Порядок и «по умолчанию» независимы. ⚠ Порядок свой у вкладки. ⚠ Селект бара - тот же порядок (`_presetBarOptions`, `_presetBarRefresh`, `_presetBarSig`). ⚠ Живой тест в `pfRunTests` рисует список ЗА ЭКРАНОМ, не `display:none`. Подробно: docs/DECISIONS.md#d-preset-order-drag-handle
- **ОКНО ПРЕСЕТОВ - ТРИ ЧАСТИ: ШАПКА, ТЕЛО, ПОДВАЛ; ПРОКРУЧИВАЕТСЯ ТОЛЬКО ТЕЛО** (задача Олега 15.09.2026): `.preset-win-head`, `.preset-win-body` (`#presetBody`), `.preset-win-foot`. ⚠ Фиксировано РАЗДЕЛЕНИЕМ, не `position:sticky`. ⚠ Кнопки формы - в ПОДВАЛЕ, разметка ОДНА - `_presetFootRender`; в `_presetFormHtml` кнопок нет (тест). ⚠ Автопрокрутка при перетаскивании (`PRESET_DRAG_EDGE`/`PRESET_DRAG_STEP`), место считает `_presetDragPlace`. ⚠ Строка ЗАЖИМАЕТСЯ в видимую область тела. Подробно: docs/DECISIONS.md#d-preset-window-three-parts
- **Активный пресет виден в селекте И суффиксом « · пресет: <имя>» в заголовке отчёта**; при расхождении отбора гаснут оба - источник ОДИН (`_presetActive`), не два.
- **ПРЕСЕТ ПО УМОЛЧАНИЮ** (`isDefault`): ОДИН на вкладку. ⚠ Применяется на ПЕРВОМ ЗАХОДЕ на вкладку за сессию (`_presetDefDone`), состояние НЕ сравнивается (15.09.2026). ⚠ Для тестов: фильтры ДО первого захода он стирает - `barRunTests` помечает вкладки «заход был». ⚠ Применяется ПОСЛЕ первого рендера. Подробно: docs/DECISIONS.md#d-default-preset-first-visit
- **СКРЫТЫЕ НАПРАВЛЕНИЯ: СПИСОК - НАСТРОЙКА В ОБЛАКЕ, ПОКАЗ - ПОЛЕ ОТБОРА.** `config/pfHiddenDirs` пишет админ; `showHidden` - фильтр вкладки, идёт в пресет. ⚠ **Скрытость в пресет НЕ переносится**: скрытие - защита. ⚠ **Форма живёт в окне пресетов**, **в шестерёнке вкладочных настроек нет**. Подробно: docs/DECISIONS.md#d-hidden-dirs-setting-vs-filter
- ⚠ **ТЕСТЫ НЕ ПИШУТ ПРЕСЕТЫ - НИ В ОБЛАКО, НИ В `localStorage`** (13.09.2026): в `pfRunTests` `_presetsSave` заглушена, семь ключей восстанавливаются, `_presets` и `localStorage` сверяются до и после. Тест, трогающий `_presets`, обязан заглушить сохранение. ⚠ Проверить `config/presets` в Firebase Console на фикстуры «т», «x», «y» (`handoff.md`). Подробно: docs/DECISIONS.md#d-tests-never-save-presets
- **МИГРАЦИЯ `config/presets`** - `presetsMigrateFields()`, как `pfFold223Sides`: чистое ядро, сухой прогон по умолчанию, запись только `{dry:false}`, только админом и после архива настроек. ⚠ Пресеты не теряются: id, имя и порядок сохраняются, меняется только состав `f`; пресет без полей остаётся. Подробно: docs/DECISIONS.md#d-presets-migrate-fields

## Шестерёнка «Настройки»: подключение, доступ, пресеты, архив

- **8 ПУНКТОВ → 5 (решение Олега 07.09.2026). В ШЕСТЕРЁНКЕ - ОБЩИЕ НАСТРОЙКИ; ВСЁ ОСТАЛЬНОЕ ЖИВЁТ НА ВКЛАДКАХ.** Порядок: Подключение к Google, Управление доступом, Пресеты отчётов, Скрытые направления (БДР), Архив. ⚠ **Шестерёнка видна ТОЛЬКО админу** - это надо помнить, решая, что сюда класть: перенесённый пункт у сотрудника не «неактивен с пометкой», а отсутствует.
- ⚠ **«Планы (БДР)» убраны как дубль**: панель открывает меню вкладки БДР. Перед удалением проверено грепом, что `showPlansPanel` больше никем не зовётся (в частности, `pfBackupPrompt` её не трогает).
- **ПРЕСЕТЫ - ТОЛЬКО ЧЕРЕЗ ШЕСТЕРЁНКУ; ПОЛОСЫ НА ВКЛАДКАХ НЕТ** (`_injectPresetBar`, `pfPresetSaveOpen` удалены). ⚠ Окно - на АКТИВНОЙ вкладке; `_presetUse` переключает И применяет. ⚠ Активный пресет виден в ЗАГОЛОВКЕ (`_presetTitleMark`, гасит `_presetActive`). ⚠ «Сохранённый пресет сразу активен» - в `_presetSave`. ⚠ Сводка фильтров - `_tabFilterSummary`. ⚠ `.preset-inline` удалены. Подробно: docs/DECISIONS.md#d-presets-only-via-gear
- **АРХИВ - ОДНО ОКНО НА ТРИ ДЕЙСТВИЯ** (`pfArchiveOpen`): «Скачать настройки» - `config/*` без `BACKUP_EXCLUDE`, только он восстановим и ставит `config/backupLog`; «настройки + данные» (`downloadBackup`) - всё плюс `RAW`, не восстанавливается. ⚠ Окно зовёт ПРЕЖНИЕ функции, `pfBackupDownloadOpen` удалено. ⚠ Бейдж устаревания в шапке БДР ведёт сюда, `pfBackupPrompt` - НЕТ; общая только `pfBackupDownload`. Подробно: docs/DECISIONS.md#d-archive-one-window
- ⚠ **Правило держится тестом**: в шестерёнке ровно пять `settings-item`, в ней нет ни планов, ни двух прежних пунктов архива; `showPlansPanel` зовут не больше двух мест, `showPresetsPanel` - не больше трёх; полосы пресетов нет ни в коде, ни в стилях. **Иголки в этих проверках склеены из половин** (`'preset-'+'inline'`): набор тестов лежит в том же скрипте и литерал целиком находил бы сам себя.

## Меню «⚙ Действия» (БДР): что видно кому

- **13 ПУНКТОВ → 3 (решение Олега 07.09.2026). В МЕНЮ ОСТАЁТСЯ ТОЛЬКО ТО, ЧТО ДЕЛАЮТ НА ВКЛАДКЕ; НАСТРОЙКИ ЖИВУТ В ШЕСТЕРЁНКЕ.** «⚠ Проверка данных (N)», «👥 ЦФО», «✏ Планы». ⚠ Права не менялись - менялся вход. ⚠ Утверждение - в редакторе планов, выписка - по имени в своде ЦФО (`cfoNameLink`). ⚠ Свод ЦФО - единственный вход в выписку, назван подписью. ⚠ Пресет БДР сохраняет 💾 в полосе пресетов. ⚠ У сотрудника пункты отсутствуют, гейты `_isAdmin()` остались. ⚠ Три сторожа брака - один пункт, пустая секция остаётся. ⚠ `pfAllocOpen` удалён целиком. ⚠ Тест: ровно три `_actItem` в разметке меню. Подробно: docs/DECISIONS.md#d-actions-menu-three-items

- **Меню видно ВСЕМ с 25.08.2026, права при этом НЕ МЕНЯЛИСЬ - изменилась только видимость.** До правки не-админ не получал меню вовсе (вместо него стояли две кнопки «👥 ЦФО» и «👁 Планы (просмотр)»), и разница между двумя людьми читалась как разные версии страницы - с этого вопроса правка и началась.
- ⚠ **ДВА КЛАССА ПУНКТОВ, И ПУТАТЬ ИХ НЕЛЬЗЯ** - разница не в оттенке формулировки, а в том, правду ли говорит подпись:
  - **Класс 1 - ограничен ПРАВИЛАМИ RTDB** (пишет в `config/*`): у не-админа НЕАКТИВЕН, приглушён, `cursor:not-allowed`, пометка «доступно администратору» - отказ пришёл бы ОТ БАЗЫ. Это утверждение месяца, сохранение пресета, «⚙ Пресеты отчётов», «📥 Восстановить из архива». Гейты `_isAdmin` ОСТАЮТСЯ. Подробно: docs/DECISIONS.md#d-lock-class-rtdb-rules
  - **Класс 2 - ЧТЕНИЕ, ограниченное было только нашим гейтом**: открыт ВСЕМ без пометок, гейты сняты (`pfNoPlanOpen`, `pfAnBadOpen`, `pfIncBadOpen`, `cfoStatementOpen`) - брак чинит бухгалтер. ⚠ Пометка «доступно администратору» без реальных прав - ложь о правах; гейты «на всякий случай» не возвращать. Подробно: docs/DECISIONS.md#d-lock-class-read-open
- ⚠ **«💾 Скачать архив настроек» - третий случай, и он назван своими словами**: правилами закрыта только ОТМЕТКА `config/backupLog`, дамп - чтение любому вошедшему. Пункт неактивен СОЗНАТЕЛЬНО, пометка «скачивает администратор» - наше решение, не права. Ключ `backup` в `PF_LOCKS`, отдельно от `rules`. Подробно: docs/DECISIONS.md#d-backup-item-third-case
- ⚠ **КЛИК ПО НЕАКТИВНОМУ ПУНКТУ ОБЯЗАН ОТВЕЧАТЬ** (`pfActionLocked`): окно называет причину и того, к кому идти (`ADMIN_EMAIL`), и перечисляет, что доступно без ограничений. Пункт, который выглядит кнопкой и молчит на нажатие, - то же молчание, с которым в проекте борются в расчётах, только в интерфейсе.
- **«✏ Редактировать планы» у не-админа называется «👁 Планы (просмотр)»** и активен: панель сама умеет режим просмотра (`window._planReadOnly=!isAdmin`). Название, обещающее правку, которой не будет, - это интерфейс, сообщающий не то, что произойдёт. Две прежние кнопки не-админа удалены: одно действие не должно жить в двух местах.
- ⚠ **Счётчик на кнопке - ОБЩИЙ по базе, а не «мои ошибки»**, и это НЕ дефект: бухгалтер видит «20 · 3 изм.» и идёт чинить в том числе не своё. Решение 25.08.2026 - счётчик не усложнять (разбираться в чужих ошибках тоже полезно), человека предупредить. Персональную фильтрацию не заводить.
- **Проверка любой правки меню**: разметка АДМИНСКОГО меню обязана совпадать с прежней ПОБАЙТНО (`actionsWrap` до и после, оба варианта `isAdmin`), иначе правка уехала в админский вид. ⚠ Вид сотрудника смотрится ГЛАЗАМИ, а не строкой разметки - подменой `ADMIN_EMAIL` в локальной копии. Подробно: docs/DECISIONS.md#d-menu-edit-bytewise-check

## Версия сборки (какую копию страницы видит человек)

- **Строка в подвале сайдбара + печать в консоль при загрузке** (`buildBadge`/`buildInfoShow`, с 25.08.2026): «Версия ДД.ММ.ГГГГ ЧЧ:ММ · `<хэш>`», по клику - окно «эта копия / репозиторий / вердикт». **Видна ВСЕМ** - шестерёнка только админу, а сравнивать версии нужно с сотрудником. Подробно: docs/DECISIONS.md#d-build-badge-visible-all
- ⚠ **Штамп НЕ вписывается в файл руками и не должен**: константу однажды забудут обновить, и она соврёт про версию. Личность копии берётся ИЗ САМОЙ КОПИИ - `document.lastModified` и `encodedBodySize`; пара «дата · размер» - то же, чем Pages помечает файл в `ETag` (проверено 25.08.2026). Подробно: docs/DECISIONS.md#d-build-stamp-not-manual
- ⚠ **Хэш коммита относится к РЕПОЗИТОРИЮ, а не к этой копии**; в файле его быть не может по построению. Спрашивается у публичного API GitHub (`/repos/OlegElis/finance-dashboard/commits?per_page=1`, таймаут 6 с); не ответил - строка короче, дата копии остаётся, окно так и говорит. Подробно: docs/DECISIONS.md#d-commit-hash-from-github
- ⚠ **Вердикт «⚠ старее» - только при отставании БОЛЬШЕ `BUILD_SKEW_MS=5 мин`**: выкладка на Pages идёт около минуты, без допуска каждый пуш зажигал бы тревогу - сторож ловит смену состояния, а не край. Второй случай назван в тексте: надпись осталась после жёсткого обновления - дело в выкладке. Подробно: docs/DECISIONS.md#d-build-skew-verdict
- ⚠ **Заголовок коммита приходит извне** и идёт в `innerHTML` только через `_htmlEsc`.
- **Диагностический факт, снятый тогда же**: Pages отдаёт `index.html` с `cache-control: max-age=600`, **сервис-воркера в проекте нет** - расхождение из-за HTTP-кэша живёт максимум ~10 минут после выкладки; дольше - только у того, кто держит вкладку открытой и не перезагружал (там работает старый JS). Подробно: docs/DECISIONS.md#d-pages-cache-no-sw

## Авторизация и доступ (Firebase Auth, email+пароль + Google)

- Дашборд закрыт экраном входа. Пользователи email/пароля заводятся **в консоли Firebase**, не в коде.
- **Белый список** (`resolveAccess` - та же функция, что резолвит роли, отдельной `checkAllowed` НЕТ): доступ если email == `ADMIN_EMAIL` ИЛИ в `config/allowedEmails`/`config/sheetAccess`. Применяется ко всем способам входа; не прошёл → `signOut` у вызывателя.
- **Доступ по ролям (вариант A)**: `resolveAccess` → админ видит `RawData`; email в `config/sheetAccess` → свою вкладку; в `allowedEmails` → `RawData`. Это разрез «у каждого свой отчёт», НЕ строгая изоляция (таблица/ключ общие).
- **fail-open**: если Firebase/CDN недоступны или нет ключей — дашборд открывается без входа (барьер, а не защита; данные и так тянутся по API-ключу из исходников).
- **Рубильник «Сбросить сессии»** (админу): пишет `config/forceLogoutAfter`, каждая сессия слушает и делает `signOut`, если метка новее её `auth_time`.
- ЦФО/ответственные = люди с доступом: список ФИО берётся из `config/names` (отдельной панели ЦФО нет - объединена с «Управление доступом» по просьбе «все, кому дан доступ, должны быть ЦФО»).
- **Конструктор запроса ЦФО** (23.09.2026): «Запрос» в панели доступа, правила-блоки, 13 полей → текст `QUERY`, в Sheets НЕ пишет; год - `CFOQ_YEAR_MODE`, живой лист: ?. Подробно: docs/DECISIONS.md#d-cfo-query-builder
- **Иерархия ЦФО (2 уровня)**: `config/cfoHierarchy` {подчинённый: руководитель}, один уровень; `_cfoHierSave` через `_fbSetSafe`. Переименование мигрирует `cfoHierarchy` И `planOwners` (`accessSavePanel`); `_planOwners[dir||подстатья]` НЕ меняется. Свод ЦФО (`pfCfoOpen`): руководитель = свои + подчинённые, `cfoToggleMgr` - DOM-тоггл; ИТОГО по СЫРЫМ `agg`. `_cfoMgrOf`/`_cfoSubsOf` игнорируют самоссылку. Подробно: docs/DECISIONS.md#d-cfo-hierarchy-two-levels
- **ВЫПИСКА ЦФО - ОТ ТЕКУЩЕГО ПЛАНА** (решение Олега 06.09.2026): план строки и база вердикта = текущий план (`fcPlan*`/`fcPlanSideMD`) по ВСЕМ месяцам; вердикт - общие `statusOf`/`PF_WARN` через `_pfStatusBase` (`stMonth`/`stYTD` в `_cfoStmtData`), своих копий светофора нет; утверждённое - колонка «Утв.» и приписка `_pfPlanRevNote`. ⚠ Печать - 8 колонок на А4, «Статус» - бейдж с суммой, процента и колонок Δ нет. ⚠ Контроль: факт ИТОГО не меняется, только план и вердикт; живой тест в `pfRunTests`. Подробно: docs/DECISIONS.md#d-cfo-statement-current-plan
- **ВЫПИСКА ЦФО: ВЫВОД ПРИБЫЛИ (127) - ОТДЕЛЬНАЯ СЕКЦИЯ «Распределение прибыли (вывод)», КАК В СВОДНОЙ** (07.09.2026). ⚠ Было: план 127 в расходах при нулевом факте - ложная экономия. ⚠ Молчаливо: порознь всё валидно. ⚠ Факт разводит `isVyvodRow` в том же проходе по `RAW`, план - `isVyvodCode`; второго прохода нет. ⚠ Код - фабрика `_vyvodCodeF(seen)`. ⚠ СТАТУСА НЕТ - остаток серым (`statusFlatOf`, `PF_FLAT`). ⚠ Секция только при `hasVyv`. ⚠ Место - НИЖЕ финрезультата. ⚠ `_cfoLastMonthWithData` смотрит и `vyv`. ⚠ Мёртвые строки скрыты (`_pfRowDead`). ⚠ Ноль у вывода - «не планировали». ⚠ Отборов ДВА: месяц+YTD и ГОД. ⚠ Фильтр на РЕНДЕРЕ, ИТОГО не двигается. ⚠ Контроль - печать без вывода ПОБАЙТНО. Подробно: docs/DECISIONS.md#d-cfo-statement-vyvod-section
- **Выписка ЦФО: расчёт и вывод РАЗДЕЛЕНЫ** (04.08.2026): считает `_cfoStmtData(params)`, потребители - `cfoStatementGen` (печать) и `cfoScreenRender` (экран). ⚠ Второй копии расчёта не заводить. ⚠ Порядок людей фиксирован: руководитель первым, признак `own`. ⚠ Суммы - `_cfoStmtData`, строки - `secRows`, обход `codes` в ОДНОМ порядке. Проверка - печатный HTML ПОБАЙТНО. Подробно: docs/DECISIONS.md#d-cfo-stmt-data-split
- ⚠ **ИЗВЕСТНОЕ РАСХОЖДЕНИЕ, НЕ ЧИНИТСЯ (аудит БДР 05.09.2026):** свод «Исполнение по ЦФО» берёт план через `planDirSubFrom`, и ГОДОВАЯ запись (`month=0`) там не делится на 12, в отличие от `_planSideMD` и сводной. Записей `month=0` нет, в числах не проявляется; чинить с первым таким планом. Подробно: docs/DECISIONS.md#d-cfo-summary-year-plan-gap
- **Экран выписки ЦФО** (`cfoScreen*`): полноэкранный слой, печать кнопкой на экране. ⚠ Слой НЕПРОЗРАЧНЫЙ. ⚠ Список остальных ЦФО не показывается. ⚠ Смена режима «свои / блок целиком» СБРАСЫВАЕТ раскрытые строки. ⚠ В режиме «все месяцы» экран показывает ОДИН месяц и пишет это. ⚠ Состав блока ПОИМЁННО. Подробно: docs/DECISIONS.md#d-cfo-screen-layer
  - ⚠ **Дашборд под экраном прячется на печати КЛАССОМ `body.cfo-print`, а не селектором `body:has(.cfo-scr)`.** `:has()` в Safari только с 15.4. Ставит `cfoScreenOpen`, снимает `cfoScreenClose`. `@media print` для экрана - ОТДЕЛЬНЫЙ блок на верхнем уровне (`@page` в Safari только там). Кегль в pt. Подробно: docs/DECISIONS.md#d-cfo-print-body-class

### Правила RTDB (Firebase Console) — задаются вне кода, без них молча ломается
- `config/.read` = `auth != null`; запись КАЖДОЙ ветки `config/*` = только `ADMIN_EMAIL` по `auth.token.email`: `plans, plansBaseline, allowedEmails, sheetAccess, names, planOwners, planNotes, rentObjects, targetLocked, presets, kassaLimits, sheets, forceLogoutAfter, bflModel, cfoHierarchy, anomalyAck, backupLog, mktCounts, accounts, dataSource, pfHiddenDirs, cfoQueries`. Подробно: docs/DECISIONS.md#d-rtdb-config-rules
- **`snapshots` - ветка данных, а не настроек, и правило у неё СВОЁ**: `snapshots/.read` = `auth != null`, `snapshots/.write` = `ADMIN_EMAIL`. Правила `config/backupLog` (27.07.2026) и `config/accounts` (03.08.2026) заведены. ⚠ `pfBackupRestoreApply` пишет `config/<ключ>` циклом: ветка без правила теряется молча - правило заводить ВМЕСТЕ с веткой. Подробно: docs/DECISIONS.md#d-rtdb-snapshots-rule
- `presence` / `activity/$ek` = `auth != null` (per-user). ⚠ `activity` ПОДРЕЗАЕТ скрипт снимка (ретенция 90 суток, см. выше) - он ходит токеном владельца проекта и правила обходит, поэтому отдельного правила записи для чистки заводить не надо; правило остаётся барьером для вошедшего постороннего.
- ⚠ Ветка без правила записи → сохранение молча запрещается. Особенно важно для `config/bflModel` (модель БФЛ) и планов.

## Ключевые учётные решения (иначе выглядят как баг и будут «исправлены» обратно)
- **РАСХОД ЛОЖИТСЯ НА ТОТ ОТДЕЛ, ЧЕЙ РЕЗУЛЬТАТ ОН ОПЛАЧИВАЕТ** (29.08.2026, модель БФЛ): квал-лид - маркетинг, сделка - ОП, встреча - КО. ⚠ Место ВВОДА ставки и ЧЕЙ расход - разные вопросы. ⚠ Проверка правки, двигающей расход между отделами: сумма по направлению, стоимость сделки и расходы БЛОКА прежние. ⚠ Выплата ЗА КОНКРЕТНОЕ направление относится к нему ЦЕЛИКОМ, не размазывается. ⚠ Деньги, ПОЛУЧЕННЫЕ от другого отдела, в расходы не входят - только справочной строкой. Подробно: docs/DECISIONS.md#d-expense-follows-result
- **«ЗА ЧТО ПЛАТИМ» И «НА КАКОЙ СТАДИИ МЕРЯЕМ» - РАЗНЫЕ ВОПРОСЫ** (29.08.2026): первый - ЧЕЙ расход, второй - КОЛИЧЕСТВО для ставки. ⚠ Агентской сети платят за СОСТОЯВШУЮСЯ ВСТРЕЧУ, но расход - МАРКЕТИНГА, не КО. ⚠ `feeTo` отвечает на ПЕРВЫЙ вопрос, для второго не годится. ⚠ Ставка называется по стадии (`feePerMeeting`, `feePerQual`, `feePerDeal`), чужое поле не переиспользовать; в строке канала стадия названа словами. Подробно: docs/DECISIONS.md#d-pay-for-vs-measure-stage
- **СТАДИИ ВОРОНКИ ПО НАПРАВЛЕНИЯМ СЧИТАЕТ ОДНА ФУНКЦИЯ - `bflFunnelStages`** (29.08.2026), зовут ОБА: `bflQtyFlow` и `bflDeptCalc`. ⚠ Две копии уже расходились. ⚠ Чинить вторым экземпляром нельзя. ⚠ Функция читает ТОЛЬКО модель и карту входа, `bflDeptCalc` не пускать; встречи маркетинг берёт из `bflQtyFlow`. ⚠ Проверка СТРУКТУРНАЯ: подмена `bflFunnelStages` меняет ОБА результата. Подробно: docs/DECISIONS.md#d-bfl-funnel-stages-one-fn
- **У КЛИЕНТСКОГО ОТДЕЛА ОДНА СТАДИЯ, И ЭТО ФЛАГ, А НЕ ВЫРЕЗАННЫЙ КОД** (решение Олега, 29.08.2026): `funnel.singleStage` ТОЛЬКО у `client_ko`, средняя стадия живёт у отдела продаж. ⚠ Вернуть стадию - флаг ПЛЮС обратное разнесение конверсии (`conv`/`otval`). ⚠ Отвал у одностадийной воронки в расчёт НЕ входит вовсе. ⚠ Стоимость промежуточной стадии - `null`, не ноль. ⚠ Проверка: НИ ОДНО число ниже по конвейеру не двигается. ⚠ Тесты воронки продаж - индикатор протечки флага. Подробно: docs/DECISIONS.md#d-ko-single-stage-flag
- **СТОИМОСТЬ ЕДИНИЦЫ НАСЛЕДУЕТСЯ ВНИЗ ПО КОНВЕЙЕРУ, А РАСХОД ОСТАЁТСЯ ТАМ, ГДЕ ВОЗНИК** (там же): собственная стоимость - расходы отдела / его результат (оценка РАБОТЫ), полная - плюс цена входа (сравнение КАНАЛОВ). ⚠ Не смешивать. ⚠ Результатов у отдела НЕСКОЛЬКО, у каждого своя пара. ⚠ Собственная стоимость последнего результата = ЦКП; вторую метрику подписывать, не пересчитывать. Подробно: docs/DECISIONS.md#d-unit-cost-inherits-down

- **ОКЛАДЫ В МОДЕЛИ БФЛ - СУММЫ НА РУКИ; ОФИЦИАЛЬНОЕ ТРУДОУСТРОЙСТВО СЧИТАЕТСЯ РАЗНАЧИСЛЕНИЕМ** (решение Олега 08.09.2026): ставка = `(1 + fss)/(1 − ndfl) − 1`. ⚠ `(1+f)/(1-n)` вместо `ndfl+fss` - НЕ ошибка, не «чинить». ⚠ Формула в ОДНОМ месте - `bflEmpTypeRate` (ставка ВИДА); `bflRoleNachRate` - ставка РАСЧЁТА, ЗОВЁТ первую. Проценты числом не пишутся. Роль без `emp` - официальная. Подробно: docs/DECISIONS.md#d-bfl-net-salary-gross-up
- **МЕСЯЦ ЗАКРЫВАЕТСЯ ПОСЛЕ 10-го ЧИСЛА СЛЕДУЮЩЕГО, и календарного «месяц кончился» НЕДОСТАТОЧНО** (29.08.2026): до 10-го прошлого месяца ЕЩЁ НЕТ. ⚠ Ошибка МОЛЧАЛИВАЯ: неполный месяц выглядит месяцем с меньшими цифрами. ⚠ В модели БФЛ - `overhead.closeDays`. ⚠ Правило шире одной карточки: скользящие средние, «месяц к месяцу», прогнозы - спрашивать «закрыт ли». ⚠ Отсечка обязана быть НАЗВАНА в интерфейсе. Подробно: docs/DECISIONS.md#d-month-closes-after-10th

- **Дашборд НЕ блокирует платёж при красном остатке, и это РЕШЕНИЕ, а не недоделка** (02.08.2026): бухгалтер видит красное и сама проверяет лимиты. ⚠ Не заводить: блокировку кнопок, `confirm()` сверх лимита, скрытие строк с перерасходом, «мягкие» подтверждения. Вся нагрузка на заметности сигнала - не ослаблять. Подробно: docs/DECISIONS.md#d-no-payment-block-red-rem
  - **Как выглядит сигнал сейчас** (02.08.2026): «Остаток» при перерасходе - число `--outcome`, «перерасход» только в подсказке; бейдж «Статус» виден без наведения. ⚠ Тот же красный у любого списания (`numClass`) - различают колонка и бейдж. ⚠ Бейдж красится `#d35450`, число - `--outcome`: два красных, сознательно. Подробно: docs/DECISIONS.md#d-overspend-signal-look
  - Оранжевый `--orange` в «Остатке» - «лимит на исходе» (остаток ≤20% плана), тоже информирование без запрета.
- **ПОЛОЖИТЕЛЬНОЕ СПИСАНИЕ - ЭТО ВОЗВРАТ, ОН УМЕНЬШАЕТ РАСХОД СВОЕГО ПЕРИОДА** (12.08.2026): примитив `expOf(r) = -(parseNum(OUTCOME)||0)`. ⚠ Условие потребителя `const v=expOf(r); if(!v)return;`, не снятие фильтра знака. ⚠ Накопители отрицательного расхода (`perOut`, `costs`) пишут `-expOf(r)`. ⚠ НЕ потребители на `Math.abs` осознанно: `_mKey`, Дебиторка БФЛ, Займы. ⚠ Проверка - разложение расхождения Банк ↔ ОПиУ на 4 компонента. ⚠ `bankSplit` тоже потребитель. Подробно: docs/DECISIONS.md#d-expof-refund-convention
- **`120_Ввод начального остатка` — НЕ выручка НИГДЕ** (глобальный хелпер `isOpeningBalance`, единообразно в ОПиУ/БДР/ФОТ/Структуре/Юнит-экономике). Это входящий остаток, а не доход; раньше раздувал ЧП и завышал %-планы. Прочие `120_` (реализация имущества, депозит, возмещение) — выручка. **ОПиУ и БДР считают выручку одинаково.**
- **«Вывод чистой прибыли» (код 127, дивиденды) НЕ входит в расходы и в ЧП** — вынесен в отдельную секцию «Распределение прибыли» ниже линии ЧП (в ОПиУ, БДР, Структуре расходов). Иначе ЧП расходилась с ОПиУ ровно на сумму дивидендов.
- **Проверка после любой правки расчётов БДР (сверка, НЕ константа)**: «ИТОГО РАСХОДЫ факт» БДР = «РАСХОДЫ» ОПиУ за тот же период и направление; проверяется РАВЕНСТВО. ⚠ Под любым фильтром БДР не проверяется - сверять со СНЯТЫМИ фильтрами. ⚠ Сравнивать БЕЗ вывода 127. Разводит аналитика вне `OPIU_CATS`; сторожа - `ancat` и «⚠ Аналитика вне ОПиУ» (`pfAnBadOpen`). Подробно: docs/DECISIONS.md#d-bdr-opiu-expense-check
- **ОПиУ, слой динамики** (`_tabFilters.opiu.dyn`, 20.08.2026): дельты под числами и блок «что изменилось»; только ЧИТАЕТ агрегаты. ⚠ Спарклайн убран, не заводить. ⚠ База - ВТОРОЙ агрегат по `baseData` без фильтра лет, одни `aggregate`/`injectPost`/`derive`. ⚠ Предыдущий период - ПО КАЛЕНДАРЮ (`basePkOf`). ⚠ Незавершённый период - УСЕЧЁННЫЙ отрезок (`shiftBackO`). ⚠ Усечение - граница в `aggregate`, не второй расчёт. ⚠ Дата отсечки из данных, в ШАПКЕ. ⚠ Прогноза НЕТ сознательно. ⚠ БУДУЩИЕ ПЕРИОДЫ НЕ СРАВНИВАЮТСЯ (неполны ПО СОСТАВУ). ⚠ Признак будущего - ПЕРВАЯ проверка `dynState`. ⚠ Прочерк в ТРЁХ разных случаях, в ИТОГО порознь. ⚠ Сравнимость - ОДИН `dynState`. ⚠ Процент от МОДУЛЯ базы; нет при нуле, < `OPIU_DYN_MIN_BASE`, смене знака. ⚠ Тип строки - `DYN_KINDS`, не знак. ⚠ Тревога - ДВА условия: ≥5 расходных И нет отрицательных; цвет гаснет, `console.warn`. ⚠ Одного положительного НЕ достаточно. ⚠ Сторож смотрит ТОЛЬКО базу. ⚠ При `dyn=''` разметка ПОБАЙТНО прежняя. Подробно: docs/DECISIONS.md#d-opiu-dynamics-layer
  - **Блок «что изменилось»**: про ОДИН период - последний СРАВНИМЫЙ (с 20.08.2026 и текущий незавершённый), оба названы в заголовке. ⚠ Два знака не путать: вклад в ΔЧП - в конвенции ОПиУ, показывается изменение строки в её терминах. ⚠ Вывод 127 в разложение не входит. Порог `OPIU_DYN_MIN_BASE` и обрезка пятёркой НАЗВАНЫ в блоке. Три равенства в ноль: Δвыручка + Δрасходы = ΔЧП; заголовок = дельте ЧП названной колонки; ΔИТОГО = Σ дельт колонок, включая усечённую. Подробно: docs/DECISIONS.md#d-opiu-what-changed-block
- **Расчётный транзит почты БФЛ↔САУ** (ОПиУ, чекбокс): виртуальные строки (в Sheets НЕ пишутся), только когда выбран ровно один контур. Кейс: БФЛ + `112_Депозиты и госпошлины` + |списание| ∈ {25000,50000} + непустой `ID_Offer` → транзит **фикс 20000 ₽/ID** (плейсхолдер; при факте заменить на фактические суммы). БФЛ −20000, САУ +20000.
- **EBITDA-корректировки** — непрофильные/разовые расходы; значение аналитики в Sheets должно быть ровно `EBITDA-корректировки`. Тумблер двигает их выше/ниже EBITDA; ЧП в обоих режимах одинакова.
- **Займы сотрудникам — знаковая модель**: `amount = inc + out`; `>0` выдача, `<0` возврат; долг = Σ amount. Нельзя брать возврат как `abs(поступления)` — иначе выдача (+) и возврат (−) оба считаются возвратом и долг задваивается.
- **Календарный `daysB`** (разница `Date.UTC` по локальным компонентам) — в Займах, Дебиторке. Прямое `(b-a)/сутки` занижало на 1 день и давало «−1 дн.» для операций «сегодня».
- **БДР: планирование ведётся ТОЛЬКО на уровне подстатьи. Уровень `article` не используется - при появлении таких записей удалять.** Уровни `subarticle`/`income`/`revenue`; строка-статья = `planAgg('subarticle',code)`. ⚠ Записи бессмертны из UI (`_planStage`, `presentKeys`) - удалять правкой `config/plans` после архива. ⚠ `_baselineBakeMonth` фильтрует по `PLAN_LEVELS`. ⚠ Отбирать по `level`, не по имени кода (`_npHasPlan`, «⚠ Факт без плана»). Контроль: Σ подстатей = статья по ОБЕИМ базам. **ЧИСТКА ПРОВЕДЕНА 31.07.2026, ОБЕ ВЕТКИ.** ⚠ Baseline тронут: записи - артефакт дефекта, не величины; удалять мутацией НА МЕСТЕ. ⚠ Точка отката - архив настроек ПЕРЕД чисткой. Контроль - `__pfSnap` до/после. Подробно: docs/DECISIONS.md#d-plan-subarticle-only
- **БДР: baseline (`config/plansBaseline`) - замороженный утверждённый план года. Не редактируется никогда.** Прогноз (`config/plans`) правится свободно, baseline - только «Утвердить план года». Δ и «Исполнение YTD» - ОТ baseline; %-планы от СВОЕЙ базы (`buildPlanCalc`); `codeSet`/`incSet`/`vyvodSet` пополняются из baseline. Флаг `retro`: заморожен, не планировался; исключён из YTD (`_planableFrozenMonths`), остаётся в Δ; пометка 📋 (вместо ⚠). Подробно: docs/DECISIONS.md#d-baseline-frozen-plan
- ⚠ **ЕДИНСТВЕННОЕ ИСКЛЮЧЕНИЕ ИЗ «baseline не редактируется никогда» - 04.09.2026, санкционировано Олегом. ПРАВИЛО ПРИ ЭТОМ ПРОДОЛЖАЕТ ДЕЙСТВОВАТЬ: редактировать baseline ПО СОДЕРЖАНИЮ по-прежнему нельзя.** В `config/plansBaseline` записи 223 со стороной свёрнуты в безымянную (`side:''`) тем же `pfFold223Sides({branch:'baseline'})`. Подробно: docs/DECISIONS.md#d-baseline-fold-223-exception
  - **Почему это НЕ нарушение правила.** Правило защищает утверждённые ВЕЛИЧИНЫ от дрейфа; свёртка тождественна - суммы по (направление, подстатья, месяц, режим) проверены ПРОГРАММНО в копейках, расхождение = отказ. Меняется только уровень группировки; `blPlanMD` и родня суммируют ВСЕ стороны. Подробно: docs/DECISIONS.md#d-fold-not-a-violation
  - **Метаданные снимков не тронуты**: `approvedAt`, `approvedBy`, `hadFact`, `factAtApproval`, `retro` как были, меняется только `records`; проверено программно. ⚠ `records` правятся МУТАЦИЕЙ объекта месяца НА МЕСТЕ - пересборка `_plansBaseline[year]` наступит на ловушку формы-массива RTDB. Подробно: docs/DECISIONS.md#d-fold-meta-untouched
  - ⚠ **Что этим исключением НЕ разрешено.** Менять величины, добавлять/удалять строки, перезамораживать месяц, править `approvedAt`/`retro` - нельзя, второго исключения по аналогии не выводить. Признак допустимости один: тождественно по суммам и не трогает метаполя, подтверждено программной сверкой. Подробно: docs/DECISIONS.md#d-fold-exception-limits
  - **Точка отката** - архив настроек, снятый ПЕРЕД прогоном (свежий: тот, что снимали до миграции прогноза, уже не отражает состояние). Датированные снимки `freeze-rawdata.gs` тут не помогают - они про `RawData`, а не про `config`.
- **БДР, режим «Статья»: строка-статья группируется по c3 подстатьи (`ARTICLE_NAME`), а не по коду статьи.** Разные статьи с одним c3 (`111_ФОТ` и `111_ФОТ (выслуга лет)`) задваивали план через `planAgg(c3)`; разворот (`subsOfArt`/`factSubN`) группирует по c3. Пустая подстатья - строка под кодом статьи (`sn=an||ac`). Фильтр по статье - по c3. Подробно: docs/DECISIONS.md#d-article-row-groups-by-c3
- **БДР, 3-й уровень (расшифровка РАСХОДНОЙ подстатьи по «стороне»)**: ФОТ → сотрудник, консалтинг → контрагент. ⚠ Аренды (223) в третьем уровне НЕТ НИГДЕ с 05.09.2026 (решение Олега). Примитивы `sidesOfSub`/`_sideUniverse`/`_buildSideFactIdx`/`_planSideMD` общие для трёх потребителей. Подробно: docs/DECISIONS.md#d-bdr-third-level-by-side
  - ⚠ **ПРЕДИКАТОВ ДВА, И ЭТО РАЗНЫЕ ВОПРОСЫ** (с 03.09.2026): `_slPlanSide` - по каким подстатьям ЗАВОДЯТ ПЛАН по стороне; `_slFactSide` - по каким РАСКЛАДЫВАЮТ ФАКТ (`_buildSideFactIdx`). ⚠ 05.09.2026 (решение Олега): у 223 третьего уровня НЕТ НИГДЕ. ⚠ Составы СОВПАДАЮТ (111 + консалтинг), но вопросы разные - НЕ СЛИВАТЬ; тест: два объявления, ни одного литерала кода аренды, 223 узнаёт только `_slRent`. ⚠ Имени `_slSub` нет НАМЕРЕННО. ⚠ Копии в редакторе (`isFotCode` и др.) и в `planPrintOwners` убраны. Подробно: docs/DECISIONS.md#d-two-side-predicates
  - ⚠ **`presentKeys` в `_planStage` собран из `dir+level+code` БЕЗ СТОРОНЫ - форма молча владеет всеми записями со сторонами под ключом, который отрисовала.** Исчезни у кода третий уровень при живых записях - «Сохранить» снесёт их: МОЛЧАЛИВАЯ ПОТЕРЯ плана. Стороны сворачивают ДО скрытия уровня; сторож в `_planStage`. Подробно: docs/DECISIONS.md#d-presentkeys-without-side
  - **Разовая миграция `pfFold223Sides`**: `{branch:'plans'|'baseline'}`, ядро `_pfFoldCore` чистое, свёртка ОДНА на обе ветки. ⚠ Второй копии свёртки не заводить. У `baseline` единица - СНИМОК МЕСЯЦА, годы ВСЕ. Сухой прогон - умолчание; запись `{dry:false}`, админом, после архива; инвариант в копейках по ВСЕМ ячейкам. ⚠ Безымянная запись могла УЖЕ быть - прибавляем. ⚠ Смешанные ₽ и % в ячейке - ОТКАЗ. ⚠ Округление до копеек В ЯДРЕ: сухой прогон печатает РОВНО то, что запишется. ⚠ Baseline свёрнут ОТДЕЛЬНЫМ прогоном 04.09.2026. Подробно: docs/DECISIONS.md#d-pffold223sides-migration
    - ⚠ **У подстатьи вне `_slPlanSide` третий уровень - расшифровка ФАКТА И ТОЛЬКО ЕГО** (04.09.2026): план, остаток и статус пусты у ВСЕХ сторон, включая безымянную; гейт `_sideNoPlan` в точке рождения плана стороны (`sidePlanP`/`sideBaseP`/`_sideOneBaseM`). ⚠ Безымянная сторона - главный случай: после свёртки весь план в `side:''`, план - на строке ПОДСТАТЬИ. Подробно: docs/DECISIONS.md#d-side-no-plan-outside-planside
  - ⚠ **Стрелка разворота исчезает, если разворачивать нечего.** Строки считаются ДО решения о стрелке (`_sideRows` → `_canOpen`), `sidesOfSub` зовётся ОДИН раз на подстатью: стрелка в пустоту - обещание, которого интерфейс не выполнит. Подробно: docs/DECISIONS.md#d-side-arrow-hidden-when-empty
  - ⚠ **Сторож `_pfSplitOff`: третий уровень обязан раскладывать факт НАЦЕЛО** - сумма фактов показанных сторон равна факту подстатьи, расхождение печатается в консоль; вынесен наверх, чтобы правило держал тест. ⚠ Округление до копеек обязательно - иначе сторож кричит от хвоста float (как у `_z0`). Подробно: docs/DECISIONS.md#d-pfsplitoff-fact-guard
  - ⚠ **Прочерк в плановых колонках третьего уровня подписан** («ведётся по направлению») в ОБЕИХ колонках. ⚠ Подпись в одной колонке рядом с прочерком в другой читалась бы как РАЗНЫЕ причины - поэтому обе. ⚠ Только вместо отсутствующего значения и только по предикату `_slPlanSide`, не по списку кодов. Подробно: docs/DECISIONS.md#d-side-plan-dash-labeled
- **БДР, СКРЫТЫЕ НАПРАВЛЕНИЯ: ИСКЛЮЧЕНИЕ ЖИВЁТ ТОЛЬКО В `dirsToAgg`** (решение Олега 07.09.2026; `config/pfHiddenDirs`). ⚠ Начального значения в коде НЕТ. ⚠ ИЗ `RAW` СТРОКИ НЕ УДАЛЯТЬ НИКОГДА. ⚠ Второй точки исключения не заводить (тест ищет проходы по `RAW` в `renderPlanFact`). ⚠ Скрытие работает ТОЛЬКО В СВОДЕ. ⚠ `allSel`/`isAll`/`dirLabel` - от ВИДИМОГО набора. ⚠ В списке скрытые ПОМЕЧЕНЫ; показ - фильтр `f.showHidden`. ⚠ Выписки ЦФО и редактора планов не касается. ⚠ Хранится массивом ЗНАЧЕНИЙ. ⚠ Правило записи завести в RTDB. Подробно: docs/DECISIONS.md#d-hidden-dirs-dirstoagg
- **БДР, ДВА НАБОРА ТЕСТОВ, И ПРИЗНАК ДЕЛЕНИЯ ОДИН** (07.09.2026): нужны `RAW` или отрисованная БДР - `pfRunLiveTests()`, иначе `pfRunTests()` (с любой вкладки, без данных). ⚠ Признак именно такой, НЕ список имён и НЕ «трогает состояние». ⚠ `pfRunLiveTests()` без условий честно отказывается, `{skipped:true}`. ⚠ Условие проверяется ОДИН раз, на входе. ⚠ `ok`/`R` - общая делянка `_pfTestBed()`. ⚠ Живые блоки стартуют с ЧИСТОЙ вкладки и возвращают её ВМЕСТЕ с активным пресетом (`_liveSnap`/`_liveClean`/`_liveBack`, 22.09.2026): `_cfoStmtData` читает `pf.dirs`, под пресетом с направлениями блок 22 расходился. Подробно: docs/DECISIONS.md#d-pf-two-test-suites
- **БДР: РАЗВОРОТ СТРОКИ НЕ СБРАСЫВАЕТ ПРОКРУТКУ** (07.09.2026). Снимок `window._pfKeepScroll` делают функции разворота через `_pfScrollSave`, возврат - в `renderPlanFact`. ⚠ Запоминают ТОЛЬКО развороты, смена фильтров - нет. ⚠ Снимок читается в ОДНОМ месте и гасится СРАЗУ. ⚠ Возврат - в `requestAnimationFrame`. ⚠ К строке не двигаем; `scrollIntoView` - только если её не видно. ⚠ Адрес строки - `data-pfkey`, ключ тот же, что в `_pfArtExp`/`_pfSubExp`/`_pfDirExp`; искать перебором, не селектором. Подробно: docs/DECISIONS.md#d-pf-expand-keeps-scroll
- **БДР, РАЗРЕЗ СТРОК - ПОЛЕ `cut`: СТАТЬИ / НАПРАВЛЕНИЯ / ЦФО** (09.09.2026). Селект в баре, поле пресета (`PF_FILTER_LABELS.cut`). ⚠ `cut='article'` не меняет НИЧЕГО - разметка побайтно прежняя. ⚠ Оба разреза - одно тело `_cutBody`/`_cutArtRow`, различие в `_CUT`; контейнер-ЦФО - `pfOwners` ПЛЮС «(не назначен)» ВСЕГДА; своего прохода по `RAW` нет. ⚠ Свёрнутый контейнер - ЧП, план ТОЛЬКО при обеих частях; сторож `_dirSplitWarn` важнее. ⚠ Родитель доходного сторожа - Σ строк таблицы (`sumIncFact`), НЕ выручка компании (13.09.2026). ⚠ Отдельная сверка выручки против строк (`_incNoCodeM`): доходная строка без кода не попадает в статьи и контейнеры. ⚠ Ключи разворотов с префиксом контейнера, `_pfResetState` чистит все. ⚠ В разрезе ЦФО «Ответственный» скрыт (`_noOwnerCol`). ⚠ Матрица «все месяцы» в разрезах отключена. ⚠ Клик в контейнере ЦФО - `[{sub,dirs}]` под ключом контейнера (`ownKey`, `pfShowTxn`). ⚠ Счётчик строк в шапке - `null`. Подробно: docs/DECISIONS.md#d-pf-cut-field
- **БДР, ЧЕТВЁРТЫЙ УРОВЕНЬ: РАЗРЕЗ ПО НАПРАВЛЕНИЯМ** под подстатьёй и под стороной (решение Олега 07.09.2026). Иерархия в режиме «Статья»: **статья → подстатья → направление**, а у подстатей из `_slFactSide` (111, консалтинг) - **статья → подстатья → сторона → направление**. Матрицы «все месяцы» уровень НЕ касается (её тело о нём не знает, и это стережёт тест).
  - ⚠ **Уровень существует ТОЛЬКО В СВОДЕ - `dirsToAgg.length>1`.** При одном выбранном направлении строка направления дословно повторила бы родителя, а стрелка обещала бы разбор там, где разбирать нечего.
  - ⚠ **НАПРАВЛЕНИЕ - ВСЕГДА СТРОКА, А НЕ ПОДПИСЬ; ОДНО НАПРАВЛЕНИЕ - ОДНА СТРОКА.** Вариант «единственное направление подписать суффиксом в лейбле» рассматривался и ОТВЕРГНУТ: одна и та же величина читалась бы двумя разными способами в зависимости от числа направлений, и человек, ищущий строку направления, не находил бы её ровно там, где она единственная и потому важна.
  - ⚠ **У СТРОКИ ОДНА СТРЕЛКА, И ЭТО НЕ УПРОЩЕНИЕ**: иерархия у подстатьи ровно одна - либо стороны, либо направления. Две стрелки в одной строке означали бы два разных разбора одного и того же числа. Подстатья `_slFactSide`, у которой сторон не осталось (гейт съел все строки), падает на разрез по направлениям - иначе стрелки не было бы вовсе, хотя разбирать есть что.
  - ⚠ **СВОЕГО ПРОХОДА ПО `RAW` У УРОВНЯ НЕТ И БЫТЬ НЕ ДОЛЖНО.** Числа - из per-dir примитивов фильтра ЦФО: `_pdFactMap`, `_sfIdx`, `_pdPlanMonth`/`_pdPlanYear`, `_fcSideMD`/`_blSideMD`. Главная проверка СТРУКТУРНАЯ (в коде нет `RAW`), совпадение чисел - вторая. Подробно: docs/DECISIONS.md#d-level4-no-raw-pass
  - ⚠ **Живые направления - общий `_rowDead`** (факт, текущий план и утверждённый - все по модулю меньше 0,005), второго порога «когда строка пуста» не заводить: разъехавшись, разрез перестал бы совпадать с родителем по составу. **Отбор идёт по `ownDirsOf(sub)`, а не по `dirsToAgg`**: под фильтром ЦФО направление с чужим владельцем не входит в число родителя, значит и строки его быть не может.
  - ⚠ **Остаток - ТА ЖЕ пара `{u,t}` и ТЕ ЖЕ `remOfSubBy`/`sideRemBy`.** База и факт по ОДНОМУ направлению, переданному параметрами (`dirs` у `_subOneBaseM`/`_subPlanM`, `factM` у `remOfSubBy`/`sideRemBy`), а не второй функцией. ⚠ Ранний выход `_subPlanM` «нет фильтра ЦФО → план целиком» при заданном `dirs` НЕ применяется. Подробно: docs/DECISIONS.md#d-level4-same-rem-pair
  - ⚠ **ОСТАТОК РОДИТЕЛЯ НЕ ОБЯЗАН БЫТЬ СУММОЙ ОСТАТКОВ ДЕТЕЙ, и это не дефект.** У направления без плана остатка нет, а его факт в остаток родителя входит (правило #1). Сторож `_dirSplitWarn` сверяет факт (общим `_pfSplitOff`), текущий и утверждённый план, а остаток - НЕТ. Подробно: docs/DECISIONS.md#d-parent-rem-not-sum
  - **Ответственный строки направления** - `_planOwners[направление||подстатья]` ровно этого направления (`ownerTd` с `dirOverride`). Владелец задан парой «направление + подстатья», стороны в этом ключе нет и быть не может - поэтому у строк направления ПОД СТОРОНОЙ проставляются `kind` и `code` (у самой строки стороны их нет: она не кликабельна и ответственного не имеет).
  - **Клик** - тот же `pfShowTxn` и тот же ключ `_pfTxn`; направление и сторона добавлены ФИЛЬТРАМИ (4-й и 5-й аргументы), а не вторым индексом: второй ключ означал бы вторую выборку тех же строк.
  - **Состояние разворота** - `window._pfDirExp`, ключ `kind|подстатья` либо `kind|подстатья|сторона` (это РАЗНЫЕ строки). Переживает ре-рендер, сбрасывается вместе с `_pfArtExp`/`_pfSubExp` в ОБОИХ местах сброса. **Счётчик строк в шапке не трогали** - он считает статьи. Подробно: docs/DECISIONS.md#d-pfdirexp-state
- ⚠ **`config/rentObjects` ЖИВ В БАЗЕ, НО НЕ ЧИТАЕТСЯ НИЧЕМ** (с 05.09.2026): ветка `_slRent(code)` в `_sideUniverse` и `isRent` в `planAddEmp` недостижимы. Решение Олега 06.09.2026: в базе ОСТАВИТЬ, не удалять. Ветки помечены мёртвыми как путь назад; в правилах RTDB и архиве - обычная ветка `config/*`. Подробно: docs/DECISIONS.md#d-rentobjects-dead-branch

## Решения пользователя по вкладкам (не откатывать)

- **Юнит-экономика (marketing)**: CPL скрыт, логика сохранена; `112_Распределение КМ` и `112_Транспортные` в CPF не входят; `300_Реклама/маркетинг` и `244_Контроль качества` не существуют. Направление - одиночный выбор. БФЛ: количества из `RawData_BFL`, `EXCL_RESP_*` вне количеств, но в дебиторке. Подробно: docs/DECISIONS.md#d-marketing-tab-decisions
- **Аномалии**: только жёсткие детерминированные правила (массив `RULES`), без статистики — статистика давала много шума. В правиле «карты АУ/САУ» (`aucard`) карта **СберБанк-1638 Елисеев ИСКЛЮЧЕНА** из `AU_CARDS` — она используется в любых направлениях, а не только АУ/САУ.
- **Аномалии, правило `outsign` - «проверь, что это возврат», а НЕ «ошибка знака»** (с 12.08.2026, см. `expOf`): подпись не называет это ошибкой. ⚠ Правило `ancat` ловит ЛЮБОЕ ненулевое списание. Положительные списания показаны с подсказкой «↩ похоже на возврат» (зеркало в `_negIdx`, `MIRROR_WIN=366`; `isRefund`), не гасит. «✓ проверено» пишет `outsign:<хэш>` в `config/anomalyAck`; только админ. Подробно: docs/DECISIONS.md#d-outsign-refund-check
- **Дубли**: сравнение ВСЕЙ строки целиком после trim, без исключений. Удаление — только вручную в Sheets (дашборд read-only).
- **Дебиторка БФЛ**: оплата (нетто) = поступления − только `112_Возвраты клиентам`; депозит суда (`120_Поступление депозита`) и перемещения оплатой НЕ считаются. Закрытые стадии ВКЛЮЧЕНЫ по умолчанию (по ним мог остаться долг). Сделки до 01.04.2024 скрыты по умолчанию (старая дебиторка фиктивная).
- **Займы**: определяются по «Реквизиты счёта» содержащему «займ»+«сотрудник»; статья/подстатья не участвуют.
- **16.09.2026: вкладки «Целевой ОПиУ» (target) и «Депозиты АУ» (depau) удалены целиком - не использовались.** Ветка `config/targetLocked` в Firebase оставлена как есть (и правило записи на неё в разделе про правила RTDB - тоже, снимать отдельной задачей).
- **ФОТ / Структура расходов**: «Вывод прибыли» исключён из расходов (иначе итог расходится с ОПиУ).
- **БДР (planfact), раскладка**: флекс-стек `#tabPlanfact`, скроллится ТОЛЬКО `.table-scroll`; `border-collapse:separate`, не `collapse`. ⚠ `overflow:hidden` на `#tabPlanfact` НЕ ставить. ⚠ Статус и «⚙ Действия» с 13.09.2026 - под «⋯» (`_pfMoreRows`). ⚠ `margin-left:auto` им не задавать. ⚠ Статус сжат до значка; `pf-actions-dd` едет с кнопкой. ⚠ ГАБАРИТЫ КОНТРОЛОВ БАРА - ОДИН ИСТОЧНИК, КЛАСС `.bar-ctl` (07.09.2026). ⚠ Новый контрол берёт класс, не копию стиля. ⚠ Высота задана ЯВНО. ⚠ Селектор - ПОСЛЕ `.dds-extra-filters select`. ⚠ Вкладок БЕЗ пресетов не касается. ⚠ `min-width:96px` у мультивыбора. ⚠ Зазор 10px группы, 6px контролы. ⚠ Полосы фильтров ПОХОЖИ - якорь по `id`. Подробно: docs/DECISIONS.md#d-planfact-layout-bar-ctl
- **Банк, раскладка и отступы**: флекс-стек, `.content:has(#tabBank:not(.hidden)){padding:12px}`. ⚠ Правило - в ОТДЕЛЬНОМ `@media screen and (min-width:821px)`; общий блок на `screen` не переводить. ⚠ Высота вкладки завязана на паддинг `.content`. ⚠ `#tabBank` - флекс-колонка, margin НЕ схлопываются. Графики в ПЕЧАТИ обязательны. ⚠ `height:100%`, а НЕ `height:auto`. ⚠ Кегль подписей в печати НЕ трогаем. ⚠ `_BANK_CHARTS` - ФУНКЦИЯ с живыми ссылками. ⚠ В печати заголовок и фильтры ОСТАЮТСЯ. ⚠ `overflow:hidden` с `#tabBank` снят 13.08.2026; числа не сокращаем. ⚠ Излишек вертикали графиков - приём `#pfChartsBody`. ⚠ Порогов в media-запросах НЕТ сознательно. Подробно: docs/DECISIONS.md#d-bank-layout-print
- **Редактор планов, «⤓ заполнить фактом»**: маркер «план ≠ факт» (подпись «факт X» + янтарная рамка) ставится ТОЛЬКО при ненулевом факте; маркер - тонкая рамка + бледный фон. `borderColor` входит в снимки undo (⤓/⌫/↶) - не терять. Строки без плана и факта - `plan-row-empty` (приглушены, НЕ скрыты). Подробно: docs/DECISIONS.md#d-plan-fill-fact-marker

- **БДР, матрица «все месяцы» - ячейка в 4 уровня**: строка отклонения только при заметном (`PF_DEV_OVER=0.03`, `PF_DEV_UNDER=0.05`). База - утверждённое при заморозке, иначе прогноз; цвет факта от ТОЙ ЖЕ базы. «Год» = Σ помесячных `devBase` (`ydb`), НЕ Σ утверждённого. ⚠ `vertical-align:top` - НЕ косметика. Жирным ТОЛЬКО «Год» и факт итогов. ⚠ Ширину задаёт строка отклонения; `colgroup` нет. Подробно: docs/DECISIONS.md#d-matrix-cell-4-levels
- **БДР, КОЛОНКА «ОТВЕТСТВЕННЫЙ» ЗАПОЛНЯЕТСЯ ТОЛЬКО У СТРОКИ, ПРИВЯЗАННОЙ К НАПРАВЛЕНИЮ** (решение Олега 09.09.2026): владелец по паре `направление||код`; у остальных пусто. ⚠ Агрегация «+N» УБРАНА (тест). ⚠ Показ решает ОДИН предикат `_ownerShown(kind,code,isChild,isGGrand,dirOverride)`; пустая ячейка сохраняет `data-label`. ⚠ ОТБОР НЕ ТРОНУТ (`subOwnerPass`/`ownersOfSub`); разметка до/после различается ТОЛЬКО в этой колонке. Подробно: docs/DECISIONS.md#d-owner-col-direction-rows-only
- ~~**БДР, колонка «Ответственный»**~~ - **ИСТОРИЯ до 09.09.2026** (первый по алфавиту + счётчик), устройство ячейки прежнее; в матрицу НЕ добавлять; источник `_planOwners[направление||подстатья]`. ⚠ Ключ - ПОДСТАТЬЯ, строка-статья агрегировала через `subsOfArt`; итоги - `dRow` с `kind=null`. Подсказка `_tipAttr` (ФИО СЫРЫМИ); `max-width:180px` + ellipsis; при фильтре ЦФО - `ownersOfRow`. Подробно: docs/DECISIONS.md#d-owner-col-history
- **БДР, сводная (месяц и год): ДВА ПЛАНА РЯДОМ и остаток к каждому.** Вторая база - «Текущий план», не «прогноз». Колонок Δ НЕТ, не возвращать. Остаток - пара `{u,t}` (`remOfSubBy`/`sideRemBy`/`remDirBy`), факт только за месяцы с базой. Цвет «Факта» и «Статус» - один `statusOf`. ⚠ БАЗА СТАТУСА - ТЕКУЩИЙ ПЛАН (`config/plans`), в ОДНОЙ функции `_pfStatusBase` (решение Олега 03.09.2026). ⚠ Изменился ровно ЗАМОРОЖЕННЫЙ месяц. ⚠ Девбаза (`_pickDev`/`_bmDev`) УДАЛЕНА ЦЕЛИКОМ. ⚠ У `_pfStatusBase` в сводной один вызывающий (`pfRunTests`). ⚠ В бейдже след пересмотра `_pfPlanRevNote`, не красится. ⚠ «Δ к утв.» - ЕДИНСТВЕННОЕ место отклонения от утверждённого, базу не трогать. ⚠ ОСТАТОК в своде ЦФО - та же база (03.09.2026). ⚠ Второй колонки «к утв.» в своде ЦФО НЕ заводить. ⚠ Приглушение остатка отделено от клика - `flat` у `remTd`. У ЧП остаток пуст, у 127 нейтрально. ⚠ `mobileCardify` матчит позиционно - рендерер ставит `data-label` сам. Подробно: docs/DECISIONS.md#d-pf-two-plans-side-by-side
- **БДР, светофор «Статуса» и подсветка «Факта»: три состояния, ЕДИНЫЙ порог `PF_WARN=0.20`, ДОПУСКА НЕТ** (с 13.08.2026). Расход: перерасход красный, остаток ≤20% жёлтый, иначе зелёный; доход зеркально. ⚠ Ровно по плану - ЖЁЛТЫЙ. ⚠ Копейка в копейку (`kop()`). ⚠ База РОВНО НОЛЬ - валидный лимит, ветка по `revenue`. ⚠ «Факт» красится только на `warn`/`bad`. ⚠ Модуль ТОЛЬКО у знаменателя (`ap=Math.abs(p)`), инвариант `done + left === p/ap`. ⚠ Процент везде - доля исполнения `f/ap`. Подробно: docs/DECISIONS.md#d-status-traffic-light-pfwarn
- **БДР, умолчание периода - ТЕКУЩИЙ МЕСЯЦ** (с 13.08.2026). Литерал в `_tabFilters.planfact` - `''`, умолчание считается в ОДНОМ месте (`renderPlanFact`) ОДИН РАЗ (`f._defPeriodDone`); год не текущий → `_pfLastMonthWithData`. ⚠ `_pfLastMonthWithData` и `_cfoLastMonthWithData` РАЗНЫЕ: первая смотрит только ФАКТ, вторая и ПЛАН. ⚠ «Исполнение YTD» от периода НЕ зависит, гейта `isYear` быть не должно. ⚠ Ряды KPI (`factExpAllM`) - БЕЗ гейта по контрагенту. Подробно: docs/DECISIONS.md#d-pf-default-current-month
- **БДР, счётчик строк в шапке считает РЕАЛЬНО ОТРИСОВАННЫЕ строки** (`_cntInc`/`_cntExp`, ставит каждая ветка, строящая `tbody`; длины `incRowsArr`/`rowsArr` не годятся). ⚠ Ветка без счётчика оставляет `null` - он не печатается: молча неверный хуже отсутствующего. ⚠ Разбивка по видам - только при реально смешанных. Подробно: docs/DECISIONS.md#d-pf-row-counter-rendered
- **БДР, пользовательские пресеты вкладки** - общий механизм `PRESET_TABS`/`_presets`, состав - белый список `PF_PRESET_KEYS`. ⚠ Служебные поля (`_defPeriodDone`) в пресет не идут: без периода умолчание работает, с периодом - перебивается (`_pfStateRestore`). ⚠ Применение ставит состояние ЦЕЛИКОМ поверх чистого (`_pfBlankState`), не `Object.assign`. ⚠ Год - признак `yearIsCurrent`, не число. ⚠ Селект возвращает MutationObserver - проверять БЕЗ ручного `_injectPresetBar`. Подробно: docs/DECISIONS.md#d-pf-tab-presets
- **«ЧЕЙ ЭТО ФАКТ» - ОДИН ПРИМИТИВ НА СВОДНУЮ, СВОД ЦФО И ВЫПИСКУ ЦФО** (09.09.2026): `_ownCodeOf(r)` = `ARTICLE_NAME || ARTICLE_CODE`, `_ownerKey(dir,code)`, `_ownerOf(dir,code)` - зовут сводная, свод ЦФО, `_buildSideFactIdx`, выписка, редактор. ⚠ Строку без подстатьи не отбрасывать. ⚠ Набор направлений в примитив не входит: сводная - `dirsToAgg`, выписка - `dirsScope`. ⚠ Копий ключа владельца (`dir+'||'+…`) быть не должно. Подробно: docs/DECISIONS.md#d-owner-primitive-one
- **БДР, фильтр ЦФО режет числа по (НАПРАВЛЕНИЕ, подстатья), а не по подстатье целиком.** Примитивы `_dirSubPass`/`ownDirsOf`, карты `_pdExp/_pdInc/_pdVyv` по `dir||sn`, `_subFactP/_subFactM/_subPlanP/_subPlanM` - через них все потребители; `subOwnerPass` только для видимости строки. Ключ карт - `sn=an||ac`, НЕ `an`; `expByDirSub` остаётся по `an`. Контроли: Σ подстатей = статья; Σ ИТОГО по ЦФО = ИТОГО без фильтра. Подробно: docs/DECISIONS.md#d-cfo-filter-dir-sub
- **БДР, фильтр «Контрагент» (`ID_SecondSide`, `_tabFilters.planfact.sides`)** - факт режется НА ВХОДЕ флагом `sp`. ⚠ `revByMonth`/`npExpMonth` НЕ режутся. ⚠ План со стороной только у `_slPlanSide`, у прочих `null` - прочерк, не ноль. ⚠ Строка-статья - прочерк, если в c3-группе есть подстатья вне `_slPlanSide`. ⚠ План без стороны - «(не распределено)», не план выбранного (`_sideUnnamed`). ⚠ Дропдаун - своя машинерия `pfSideRender`/`pfSideToggle`, в DOM ≤ `PF_SIDE_MAX=200`, выбор в `_tabFilters`. Подробно: docs/DECISIONS.md#d-pf-side-filter
- ~~**БДР, разрез «По направлениям»**~~ - УДАЛЁН 07.09.2026 (решение Олега), заменён ЧЕТВЁРТЫМ УРОВНЕМ; снесены `rowDim`, `pfSetRowDim`, `dirTbody`, `_sideDir*`. ⚠ Старые пресеты с `rowDim` применяются - поле лежит в состоянии, не читается, в «Сбросить (N)» не идёт. ⚠ Контроль правки - хэш строк сводной до и после. Подробно: docs/DECISIONS.md#d-pf-rowdim-cut-removed
- **БДР, KPI-карточки - ВСЕГДА по компании** (`_codeAll`/`_incAll`, `revByMonth`/`npExpMonth`, `planExp`/`bPlanExp`): фильтры на них не влияют, иначе ЧП смешала бы полную выручку с отфильтрованными расходами. Пояснение `KPI_NOTE` - в `title` плиток и значком ℹ; полоса под баром убрана 14.09.2026, текст не сокращать, новых полос не заводить. Суммы по фильтру - строки «ИТОГО … по фильтру»; ЧП по фильтру не показываем. Подробно: docs/DECISIONS.md#d-pf-kpi-company-wide

## Ловушки по вкладкам (корректность/UX — не «упрощать»)

- **Реестр, ширины колонок: пиксели в JS (`_regApplyColW`), проценты не возвращать.** Атомарные колонки не ужимаются никогда, кодовые фиксированы, прирост окна - четырём текстовым по весам. ⚠ Мерить ширины ТОЛЬКО по `scrollWidth` ячейки, не `canvas.measureText` - канвас не применяет `tabular-nums`. Подробно: docs/DECISIONS.md#d-reg-colw-pixels
  - **Минимум текстовой колонки 50px - ЖЁСТКИЙ.** Раньше он проседал до равной доли остатка, и при 1280 с активным поиском колонки молча получали 32/32/32/33px, а при 980 - ноль, то есть таблица теряла четыре колонки и никак об этом не сообщала.
  - ⚠ **Ниже порога включается ГОРИЗОНТАЛЬНАЯ ПРОКРУТКА - осознанное исключение из правила «прокрутки нет»**: на узком окне выбор между прокруткой и исчезновением четырёх колонок. Порог считается ОТ ВЕЛИЧИН (`need = фикс. часть + 4×50`), числом не зашивается - меняется от любой правки ширин. Подробно: docs/DECISIONS.md#d-reg-hscroll-exception
  - **Разворот кодовых колонок в полное значение ОТВЕРГНУТ 02.08.2026, не поднимать.** Пять полных значений стоят +557px, а взять их можно только прижав текстовые к минимуму: «Назначение» с 277 до 125, «Реквизиты счёта» с 190 до 56 (замер при 1920). Обмен худшего на плохое.
  - **Подписи шапки: «Статья», «Подстатья», «Аналитика» - полные, «Напр.» и «Фин.» - сокращённые.** После снятия прописных и разрядки подпись стала уже примерно на 15%: «Статья» влезла в прежние 52px бесплатно, «Подстатье» и «Аналитике» добавлено 8 и 11px. Полным «Направление» и «Фин. операция» нужно ещё 31 и 44px - дорого.
  - **ID Offer показывается ЦЕЛИКОМ**, обрезает ширина колонки с многоточием. Отрезание третьей части (ФИО) заводилось при колонке 80px и пережило причину - сейчас колонка 164px при 1600 и 260px при 1920.
- **Реестр, быстрый поиск: сопоставление НЕ подстрокой. У текста и у чисел правила РАЗНЫЕ, и это не непоследовательность.** Примитивы `_regNorm` и `_regHasWord(norm,tn,word)`; через них ВСЕ три потребителя - отбор, подсветка, колонка «Совпало», иначе разойдутся. Подробно: docs/DECISIONS.md#d-reg-search-primitives
  - **Токен сопоставляется с НАЧАЛОМ слова, а не с любой позицией.** Подстрока давала «рав» внутри «упРАВляющих»: «тойота рав 4» возвращало 11 строк при четырёх настоящих.
  - **ЧИСЛОВОЙ токен - целым словом, с границами с ОБЕИХ сторон** (`_regFreeTok`: `/^[\d ]+$/` по нормализованной форме → `word=true`). Префикс для чисел врёт: «15 000,00» находило 150 000 и 1 500 000 - **154 ложные строки из 2 731** (5,6%), тот же класс мусора, что «рав» в «управляющих». После правки 2 577; 150 000 и 1 500 000 находятся своими запросами.
  - **ТЕКСТОВЫЙ токен остаётся ПРЕФИКСНЫМ и переводить его на целое слово НЕЛЬЗЯ**: «иван» обязан находить «Иванову», «рав» - «рав4» слитно; префикс делает отбор устойчивым к тому, как написано в источнике. Подробно: docs/DECISIONS.md#d-reg-text-token-prefix
  - **Копейки в выдаче ОСТАЮТСЯ сознательно**: «15 000,00» показывает и 15 000,50 - в блобе сумма лежит как «15000 5», копейки идут отдельным словом. Соседняя операция того же порядка полезнее отсечённой. Отсечь можно только числовым сравнением - это отдельная ветка для колонок сумм со своим поведением у подсветки и «Совпало», цена несоразмерна.
  - **В отбор не идут короткие ЧИСЛА (1-2 цифры) и русские СЛУЖЕБНЫЕ слова в одну-две буквы** (`REG_STOP`) - иначе союз «и» молча сужал выдачу. Механизм один (`hlOnly`), признак `stop` - для счётчика. ⚠ Список строго из одно-двухбуквенных; длинными словами не расширять. Подробно: docs/DECISIONS.md#d-reg-stop-words
  - **Запрос ИЗ ОДНИХ таких токенов ищется как отдельное слово** (`_regPromoteShort`, общий для чисел и стоп-слов): иначе отдавал бы ВСЮ базу; счётчик пишет «искалось как отдельное слово». ⚠ Признак продвижения - отдельный `promoted`, а НЕ `word`: `word` стоит у любого числового токена. Подробно: docs/DECISIONS.md#d-reg-promote-short
  - ⚠ **Регулярки ТОЛЬКО с флагом `u`** - без него `\p{L}`/`\w` кириллицу не матчат (на этом уже горели с «Картой ФЛ» в Банке).
  - ⚠ **Следствие, принятое вместе с правилом чисел: КУСОК номера в быстрой строке больше не ищется** - «1822» не найдёт 18224, ищется номер целиком. Прицельный `id:1822` работает по-старому, префиксом: **синтаксис `поле:значение` этих правил не касается вообще** (`_regFreeTok` ставит признаки только при `col==null`).
  - ⚠ Даты лежат в индексе ТОЛЬКО как дд.мм.гггг: пока там был Excel-серийный номер, «08» находил 26.04.2024 (в 45408 есть «08») и строка выглядела пустой.
- **БДР: «плана нет» ≠ «план равен нулю». В итогах НЕЛЬЗЯ `reduce((s,r)=>s+(r.plan||0),0)`** - Δ итога шла бы от нуля. Слой планов держит `tot=null`; в итогах - `sumOrNull(arr,get)`. Нулевой план валиден: проверять `!=null`, не truthiness. В «Остатке» `hasP=planM!=null`; «лимит на исходе» - guard на ненулевой план. Подробно: docs/DECISIONS.md#d-pf-null-plan-not-zero
- **БДР: НУЛЕВЫЕ ПЛАНЫ НЕ ТРОГАТЬ** (решение Олега 06.09.2026). Явный ноль - настоящий план; факт при нём - перерасход, не скрывается (`statusOf`). ⚠ Массовая чистка нулей ЗАПРЕЩЕНА: «нулевая запись» и «записи нет» - разные состояния (`_planStage`). ⚠ «⤓ Заполнить фактом» пишет 0 в месяцы без факта НАМЕРЕННО. Подробно: docs/DECISIONS.md#d-pf-zero-plans-keep
- **ОСВ, переходящее сальдо**: скрытые периоды ДО первого видимого вкатываются в «Сальдо нач.» ТОЛЬКО в `rowCalc`; цикл `buildRow` стартует с `lastVisIdx = pksAll.indexOf(visPks[0]) − 1` и докатывает лишь скрытые периоды МЕЖДУ видимыми. Иначе при обрезке разреза «день» (лимит 62 дня) получается двойной счёт сальдо.
- **Банк: ПОТОК и ЗАПАС - величины разной природы, и период применяется ТОЛЬКО к потоку.** Поток - обороты и графики; запас - Сальдо, Cash Runway, Лимиты, «Итого» сводов. ⚠ Остаток, ограниченный периодом, - уже ИЗМЕНЕНИЕ остатка. Запас - из `stockRows` (те же фильтры, БЕЗ дат), поток - из `data` (`sInc/sOut/sMov`). Подробно: docs/DECISIONS.md#d-bank-flow-vs-stock
  - **Умолчание периода - ТЕКУЩИЙ ГОД** (`bankDefaultPeriod`, одна функция на два вызова: старт вкладки и общая «Сбросить (N)» - сброс обязан возвращать туда же, откуда вкладка открывается). Верхняя граница - конец данных внутри года, а НЕ сегодня: иначе фильтр дат резал бы будущие оплаты и тумблер «Будущие оплаты» стал бы немым. Нет данных за текущий год - остаётся весь учёт.
  - **Контрольное правило: Σ «Итого» по трём сводам = Сальдо в полосе.** Держится при любом периоде и фильтре ФНС. ⚠ Видимость счёта: движение за период ИЛИ ненулевой остаток с начала учёта (`hasMove` смотрит и на `a.total`), иначе рубли счёта остаются в Сальдо, а сам он пропадает из свода. Подробно: docs/DECISIONS.md#d-bank-total-equals-saldo
  - ⚠ **Признак ФНС берётся по ВСЕМ строкам счёта, а не по срезу периода** - он свойство СЧЁТА (то же правило уже действует в карточке «Доля оформленного»). По срезу счёт, у которого «да» стоит только в строках 2024 года, при периоде «текущий год» уезжал бы в другую карточку.
  - ⚠ **Источников признака ДВА, и порядок фиксирован** (`_accFnsOf`, с 03.08.2026): запись справочника с `ok===1` перебивает факт из строк, `ok:0` - нет. Правило одно на весь Банк. Подмена заметна: «спр.» в своде, в инвентаризации счётчик и ⚠ в колонке ФНС. ⚠ Пометка ПЕРЕД именем: `td.bank-name` обрезает справа. Подробно: docs/DECISIONS.md#d-acc-fns-two-sources
  - ⚠ **Cash Runway period-независим В ОБЕИХ половинах**: расход для среднего берётся из `stockRows`, а не из `data`. Окно «90 дней до конца прошлого месяца» фиксировано - при периоде «этот месяц» оно вообще не попадало бы в срез, и Runway уезжал бы вместе с фильтром.
  - **Runway: расход и возвраты копятся ВРОЗЬ** (`out90`/`ret90`/`base90`; с 12.08.2026); разложение у обоих потребителей (`_tipAttr`, попап). ⚠ Потолок «> 10 лет» (`RUNWAY_CAP=3650`) - ТОЛЬКО с подписью. ⚠ У прочерка ДВЕ причины: «Нет данных» и «Возвраты выше расхода». ⚠ Метка, цвет и причина - ОДИН хелпер `_runwayView`. Подробно: docs/DECISIONS.md#d-runway-out-ret-apart
  - **Подписи под цифрами ОБЯЗАТЕЛЬНЫ** («за янв-авг 2026» у потока, «на дату» у запаса), то же словами в шапке каждого свода и строке итога. Подпись потока - по МЕСЯЦАМ, реально попавшим в выборку, а не по границам фильтра. Дата запаса - сегодня, при включённых будущих оплатах - последняя дата набора. Подробно: docs/DECISIONS.md#d-bank-period-captions
  - **Дельта к тому же отрезку прошлого года** у Поступлений и Списаний - в ПРОЦЕНТАХ, рост расхода - ухудшение. ⚠ Верхняя граница окна прижимается к последней дате с данными. ⚠ Сравнение показывается ТОЛЬКО если сдвинутое на год окно целиком лежит РАНЬШЕ текущего - иначе нахлёст сам на себя. Подробно: docs/DECISIONS.md#d-bank-yoy-delta
  - **Тумблер «Будущие оплаты» по умолчанию ВКЛЮЧЁН, и это проверено цифрой, а не привычкой** (замер 02.08.2026: доля будущих строк ничтожна). ⚠ Условие возврата к вопросу: РЕГУЛЯРНЫЕ будущие оплаты - тогда завышенная дельта к прошлому году, непроведённые платежи в Сальдо и «Итого», сдвиг Cash Runway. Лечится исключением будущего из ЗАПАСА или выключением тумблера; проверять тем же снимком. Подробно: docs/DECISIONS.md#d-bank-future-toggle-on
  - ⚠ **Дата запаса при ВЫКЛЮЧЕННОМ тумблере «Будущие оплаты» - минимум из верхней границы и сегодня.** Умолчание тянет границу к последней дате данных, а там бывает завтрашний плановый платёж: подпись брала границу напрямую и обещала «на 03.08.2026» при остатке, посчитанном по сегодня - число верное, дата на день вперёд. Граница В ПРОШЛОМ при этом остаётся датой запаса в обоих режимах.
  - ⚠ **«Счетов» стоит среди ПОТОКОВЫХ карточек и подписан периодом**, хотя по смыслу гибрид (счёт входит в своды при движении за период ИЛИ ненулевом остатке). Чистым запасом её не сделать - она равна числу строк в сводах. Строка «За весь период учёта…» и `.bank-allperiod` удалены. Подробно: docs/DECISIONS.md#d-bank-accounts-kpi-flow
- **Банк, extra-фильтры**: списки значений в дропдаунах строятся из `dataBase` (снимок ДО применения extra-фильтров), чтобы выбор одного значения не «схлопывал» список остальных.
- **Банк, фильтр-бар: «ID Offer» и «Контрагент» УДАЛЕНЫ СОЗНАТЕЛЬНО, не возвращать.** Поиск по ним - в Реестре. Обёртки `bankOffer*`/`bankSecond*` сняты, `_bankExtra*` остались. Порядок: Реквизиты счёта · ФНС · Направление · Будущие оплаты. Старые пресеты с `offerIds`/`secondSides` применяются: лишние ключи не читаются. Подробно: docs/DECISIONS.md#d-bank-bar-offer-side-removed
- **Банк, ПЕРЕМЕЩЕНИЯ между своими счетами - отдельная сущность и на графиках, и в своде.** Перемещение - пара в колонке поступлений, схему не менять. Единый разбор - `isMoveRow`/`bankSplit` во всех точках агрегации (`bankRebuildChartData`, `bankUpdateDays`). Цвет `BANK_MOVE_COLOR`, НЕ `--orange`. «Остаток ДС» НЕ меняется. ⚠ Две ловушки: `hasMove` учитывает `mov` (иначе счёт с одним транзитом пропадает), `saldo` включает `totMov` (иначе под фильтром счёта расходится с «Итого»). Подробно: docs/DECISIONS.md#d-bank-moves-separate-entity
- **Банк, дневной график: клик по столбику НЕ уводит с вкладки.** `bankDayToRegistry` удалена, `#chartMonth{cursor:default}` (общий `.chart-wrap canvas` не трогать); клик по месяцу оставить. ⚠ Списания в датасете ПОЛОЖИТЕЛЬНЫЕ (`bankSplit`), в итоге ВЫЧИТАЮТСЯ. ⚠ Валовые `dayMovIn`/`dayMovOut` передаются в `chartDays` АРГУМЕНТАМИ (в датасете сальдо); сторож `console.warn`, массивы в `_bankChartData`. Подробно: docs/DECISIONS.md#d-bank-day-chart-no-nav
- **Банк, KPI «Доля оформленного»**: поступления по счетам ФНС=Да к базе четырёх каналов; перемещения исключены (`isMoveRow`). ⚠ Фильтр ФНС на карточку влиять не должен - под фильтром прочерк. Светофор только у стрелки. В названиях не использовать «официальный», «неофициальный», «серый» - точная: «до налогового оформления». Подробно: docs/DECISIONS.md#d-bank-kpi-formalized-share
- **Банк, «Доля оформленного» - ДВА режима, и сравнение только там, где базы одинаковые.** Режим 1 (сравнение): период не задан или пресеты «Этот год»/«Прошлый год»; окно - янв-<последний месяц С ДАННЫМИ>; база - ПРЕДЫДУЩЕЕ КАЛЕНДАРНОЕ ПОЛУГОДИЕ (`_halfBefore`). ⚠ Год-к-году отвергнут, не возвращать. ⚠ «Предыдущие N месяцев подряд» тоже отвергнут. Исключение - пресет «Прошлый год»: там год-к-году, это НЕ недоделка. ⚠ Разная длина отрезков допустима, подписи называют ОБА. Режим 2 - произвольный период, без сравнения. ⚠ Режим определяется `preset`, а НЕ непустотой дат (`buildFilterOptions` заполняет их всегда). Подробно: docs/DECISIONS.md#d-formalized-share-two-modes
- ⚠ **Признак ФНС для карточки берётся из карты «счёт → признак» по ВСЕМУ набору без дат**: признак принадлежит счёту. Известное следствие, НЕ баг: свод берёт признак по видимым строкам, карточка и свод могут разойтись - дефект данных. ⚠ С 03.08.2026 признак даёт `_accFnsOf` - подтверждённая запись справочника перебивает факт; карта по строкам - второй источник. Подробно: docs/DECISIONS.md#d-fns-flag-from-full-set
- ⚠ **«Доля оформленного»: ВВОД НАЧАЛЬНОГО ОСТАТКА ИСКЛЮЧЁН из базы. Решение принято осознанно 31.07.2026, оно расходится со вторым правилом проекта - не «упрощать» обратно.** Карточка мерит входящий поток, остаток потоком не является. ⚠ Исключение - ТОЛЬКО в базе карточки (`_isOpenBal` в `_chanOf`); свод, сальдо и KPI Банка не трогает. ⚠ Канал «прочие расчёты» сейчас ПУСТ, это норма - строка остаётся сторожем: непустой значит счёт вне шаблонов и `BANK_ACC_CHANNEL_FIX`. Подробно: docs/DECISIONS.md#d-opening-balance-out-of-share
- **Банк, канал счёта - одна функция `bankAccChannel(name,fns)`**. ⚠ Порядок: сначала ФНС, потом имя: `fns===1` → `'da'`; далее карта/касса/`'unknown'`. ⚠ `'unknown'` = «по имени НЕ РАСПОЗНАН», в подсказке «прочие расчёты», входят в базу. ⚠ Классификация по имени хрупкая - новое имя в `'unknown'` пишется в `console.warn`. ⚠ Регекс кириллицы - `[а-яё]`, а НЕ `\w`. Счета вне шаблонов - ЯВНЫМ списком `BANK_ACC_CHANNEL_FIX` там же: ключ - имя в нижнем регистре, совпадение полное; ФНС=Да не перебивается. Подробно: docs/DECISIONS.md#d-bank-acc-channel-one-func
- ⚠ **ИЗВЕСТНОЕ ОГРАНИЧЕНИЕ: в `RAW` НЕТ признака вида счёта.** Канал угадывается по названию; явный список - заплатка. План устранения: колонка «Вид счёта» в источнике из справочника, классифицировать по ней. До этого новый счёт с непривычным именем молча попадает в «прочие расчёты» - следить за консолью. Подробно: docs/DECISIONS.md#d-raw-no-account-kind
- **Справочник счетов (`config/accounts`, шаги 1-6 в коде).** Читают KPI «Счетов» (подтверждённые записи) и признак ФНС (`_accFnsOf`). Канал счёта по ВИДУ пока НЕ подключён - `bankAccChannel` угадывает по имени; «Карта АУ», «Корпоративные карты», «Топливные карты» отложены решением 03.08.2026 - решает человек. Подробно: docs/DECISIONS.md#d-accounts-directory
  - ⚠ **Поля «направление» в карточке счёта НЕ заводить.** Счёт не привязан к одному направлению: в «БФЛ - Касса» 3 432 строки БФЛ и 1 378 АП - поле врало бы на четверти строк. Направление остаётся свойством ОПЕРАЦИИ, а не счёта.
  - **Признак «активен / закрыт» - РУЧНАЯ отметка, не автоматика по дате последней операции.** Автомат ошибётся на редких, но живых кассах (у «БФЛ - Касса КО» 236 строк за два года, идёт непрерывно) и на сезонных. Дату последней операции показывать рядом с отметкой как подсказку - решает человек.
  - **Виды**: ООО, ИП, Карты физлиц, Карта АУ, Кассы (в т.ч. «Задолженность по страховым взносам», «Займы сотрудникам»), Топливные карты, **Депозиты**, **Прочие юрлица** («р/с», а НЕ префикс), **Корпоративные карты** («Карта ГК ОПОРА» не юрлицо). Пустое имя не заводить (`if(a.raw==='')gAlloc.push(a)`). Подробно: docs/DECISIONS.md#d-account-kinds
  - ⚠ **В отчёте инвентаризации НЕ показывать автоматических вердиктов про дубли** - только даты и объёмы, решает человек. Правило «периоды не пересекаются = переименование» верно только для пар, у которых есть основание быть одним счётом; на живых данных оно дало два ложных вердикта («Касса ОС ↔ КО2», «КО2 ↔ АО» - просто разные мелкие кассы, не совпавшие по времени).
  - ⚠ **Разбор имени - только подсказка при заведении счёта, вид хранится в справочнике.** Иначе переименование в источнике молча меняет вид. ⚠ И `\b`, и `\w` в JS остаются ASCII-only ДАЖЕ с флагом `u`; правая граница слова - через `(?![\p{L}\p{N}])`. Подробно: docs/DECISIONS.md#d-name-parse-hint-only
  - ⚠ **Ключи RTDB**: слэш есть минимум у 10 счетов (восемь «р/с» у ООО и ИП, «АРИБ - р/с СберБанк», «S7 - Касса (№QY74954/CL)») - кодировать `_rtdbEncObj`. Правило записи `config/accounts` заведено 03.08.2026 (см. раздел «Правила RTDB»).
  - **Замещение ОИ → ОС** (не переименование, счета РАЗНЫЕ): «БФЛ - Касса ОИ» шла по 80-220 строк в месяц и обвалилась до единиц ровно в сентябре 2025, когда открылась «БФЛ - Касса ОС» (249 строк в первый месяц). Ответственные и подстатьи разные, поэтому в справочнике это два счёта, но ОИ помечается закрытой.
  - **«БФЛ - Касса ОП» и «(Ватутина 27)» - ДВА СЧЁТА** (решение 02.08.2026), не сливать. Пересекаются 9 месяцев, обе живы в августе 2026; ОП падает 62→31 строки в месяц, Ватутина растёт 29→70, ответственный у обеих один (Кривчикова) - это переезд потока, но точки разные.
  - **Базовая «БФЛ - Касса» остаётся КАК ЕСТЬ** (решение 02.08.2026): 5 830 строк, 55 подстатей, два направления (3 432 БФЛ + 1 378 АП). Разносить её по отделам не будем - это общая касса, и в справочнике она обычный счёт вида «Кассы».
- **Банк, ширины колонок свода**: классами `col.c-name`/`col.c-num`, НЕ инлайном. По умолчанию проценты, от 1600px `c-num:128px` + `c-name:auto`. ⚠ `width:min(19%,120px)` в `<col>` НЕ РАБОТАЕТ: при `table-layout:fixed` Chrome игнорирует `min()`/`max()`, `calc()` работает. На печати - одна колонка (`bank-cols{grid-template-columns:1fr}`). Подробно: docs/DECISIONS.md#d-bank-col-widths-classes
- **Банк, клик по строке счёта - НЕ то же, что фильтр «Реквизиты счёта».** Клик пишет в `_bankFilter` и меняет только графики; список - в `_tabFilters.bank.accounts` и режет весь отчёт. ⚠ `bankUpdateDays` обязана применять `_bankFilter.accs` тем же вторым шагом, а `bankRebuildChartData` - звать её. Подробно: docs/DECISIONS.md#d-bank-row-click-vs-filter
- **Банк, фильтр «ФНС» режет СЧЕТА по их признаку, а НЕ строки.** Признак счёта - из его строк (первое «да»/«нет», ни одного → `-1`), значения `''`/`da`/`net`/`unk` (`BANK_FNS_OPTS` - один источник для бара и пресета). Суммы внутри счёта от фильтра не зависят, меняется только состав видимых счетов. Подробно: docs/DECISIONS.md#d-bank-fns-filter-accounts
- **Пресеты Банка** (`PRESET_FIELDS.bank`): Название · Период · Направление · Реквизиты счёта · ФНС · Будущие оплаты; счета из `RAW` (`_presetAvailAccounts`), механика `bankDDFilter`. ⚠ Имена счетов содержат «/» и лежат ЗНАЧЕНИЯМИ массива, не ключами объекта - ключом уронило бы запись `config/presets`. ⚠ `data-str="1"` на мультиполе счетов: иначе числовой счёт станет `Number` в `_presetSave`. Подробно: docs/DECISIONS.md#d-bank-presets-fields

## Модель БФЛ (вкладка bflmodel)

Параметрическая сценарная юнит-модель («к чему стремиться»), НЕ путать с вкладкой «Юнит-экономика» (marketing). Детали структуры — в коде и в ТЗ (`ТЗ_Юнит-экономика_БФЛ`, `ТЗ_ClaudeCode_БФЛ`). Ключевое:
- **Эталон ТЗ §6 содержит 2 несоответствия** (разобраны, не «чинить»): (1) §5.4 «ОС Расходы 3 306 194» — опечатка, из строк выходит 3 036 194; (2) депозит pass-through по числу эталона фактически НЕ вычитается (асимметрия в самом ТЗ). Модель по умолчанию **включает депозит** (как эталон); тумблер даёт net-вариант.
- Накладные (аренда/коммуналка/эквайринг) убраны из всех отделов по просьбе — только ФОТ + прямые затраты. Из-за этого ЦКП отделов отличаются от эталона §5; тесты `bflRunTests()` проверяют маркетинг (2700) и выручку точно, расходы — диапазоном.
- ФОТ ОМ входит в расходы маркетинга («моделируем правильно»). Контур ОПАУ — самоокупаемый, со своим ОПИУ, исключён из общего `bflOPIU`.
- **Авто-сохранение** при каждой правке в `config/bflModel` + localStorage; Firebase-listener применяет облако только если `savedAt` не старше локального (иначе затирал бы свежие правки).

## Роадмап
- ~~Названия в третьем уровне 223 обрезаются колонкой~~ - **сделано 05.09.2026 в СВОДНОЙ БДР** (`_pfTrimCommon`): общий префикс сторон срезается, полное значение в подсказке (`opts.fullLabel`). ⚠ Случай, ради которого заводилось, с 05.09.2026 ИСЧЕЗ (у 223 третьего уровня нет). Механизм остаётся общим (`_pfTrimCommon`/`_pfTrimNote`); фикстуры тестов - стороны консалтинга. Подробно: docs/DECISIONS.md#d-trim-common-prefix
  - ⚠ Граница среза - **сильный разделитель (запятая/точка с запятой/слэш), а НЕ любой пробел**: срез по пробелу оставил бы голые числа. Нет сильного разделителя в общем префиксе - **не режем вообще**: неудачное сокращение хуже длинного. Подробно: docs/DECISIONS.md#d-trim-strong-separator
  - ⚠ Порог `PF_PREFIX_MIN=12` - чтобы случайное совпадение первых букв не давало «…ов» / «…енко». Безымянная сторона в расчёте префикса НЕ участвует (иначе одна такая строка обнуляла бы префикс и правило молчало бы всегда), схлопывание строки в пустоту отменяет срез целиком.
  - **Доведено до бумаги 05.09.2026** (выписка ЦФО и печать плана). Три вещи, каждая со своим поводом:
    - ⚠ **На листе имя стороны не режется, а ПЕРЕНОСИТСЯ** (`td.nm{white-space:normal}`), задача там другая. Срез делает ТА ЖЕ `_pfTrimCommon`; `_pfTrimNote` вычисляет префикс ИЗ ЕЁ РЕЗУЛЬТАТА (`pre.length = s.length - (disp.length - 1)`), а не своей формулой - иначе подпись назвала бы не то, что срезано. Подробно: docs/DECISIONS.md#d-print-trim-note-from-result
    - ⚠ **Срезанный префикс на бумаге НАЗЫВАЕТСЯ ОДИН РАЗ** - подписью под таблицей секции: на листе нет `title`, молча срезать = выкинуть адрес. Подпись печатается ТОЛЬКО при состоявшемся срезе; хвостовая пунктуация убирается (`[,;\s]+$`). Несколько префиксов - каждый своей строкой с названием подстатьи. Подробно: docs/DECISIONS.md#d-print-prefix-named-once
    - ⚠ **Выписка ЦФО повторяла дефект «(не распределено) с полным планом»** - гейт `_slPlanSide` в точке рождения плана стороны (`sideBlMonth`/`sideFcMonth` в `_cfoStmtData`), а не в местах вывода: закрывает план, Δ и отбор строк. **ИТОГО секций от гейта не двигается по построению** - держит тест. Подробно: docs/DECISIONS.md#d-cfo-stmt-side-plan-gate
    - ⚠ **`planPrintOwners` держала ТРЕТЬЮ копию правила** «у каких подстатей есть сторона». Копия удалена, зовётся общий `_slPlanSide` (это ПЛАН - лист печатает `_planRecords`). **Цифр кода подстатьи в этой функции больше нет, и это стережёт тест**: цифры там означают, что правило снова завели копией. Подробно: docs/DECISIONS.md#d-plan-print-no-code-digits
  - **ОСТАЛОСЬ:** обрезка в **экранной** выписке ЦФО не нужна - третьего уровня там нет вовсе
    (`cfoScreenRender` идёт направление → человек → подстатья → операции). Возникнет он там - обрезка
    и подпись берутся готовыми (`_pfTrimNote`), новой реализации не заводить.
- Колонка «Вид счёта» в источнике (справочник + выпадающий список) - чтобы канал в Банке («оформлено / наличные / карты физлиц») не угадывался по названию счёта. Пока - шаблоны имён + явный список `BANK_ACC_CHANNEL_FIX`.
- ~~Перенести `_mktCounts` в `config/mktCounts`~~ - **сделано 02.08.2026** (см. раздел «Данные и источник»). Apps Script Web App отвергнут: механизм `config/*` уже отлажен, а он был бы отдельным сервисом со своим деплоем, правами и точкой отказа ради десятка чисел в месяц.
- ~~Подключить источник для количеств лидов/сделок/дел~~ - **закрыто 31.07.2026**, причины в разделе «Данные и источник» (направление сделки без правила разнесения не определяется).
