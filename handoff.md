# Передача дел - 03.08.2026

> Состояние проекта на конец сессии 3 августа. Проверено ПО КОДУ, а не по памяти разговора.
> Устройство вкладок, имена функций и учётные правила - в `CLAUDE.md`, он первичен.
> Здесь - только то, чего в нём нет: где остановились, что открыто и чем это чинить.

## Где остановились

Сессия 3 августа целиком ушла на **справочник счетов** (`config/accounts`): шаги 1-6, правило
записи в Firebase Console и оптимизация признака ФНС. Последний коммит - `318fc7e`.
Дерево чистое, всё запушено. ⚠ Два последних коммита датированы 04.08 - работа шла после
полуночи, это та же сессия.

Что справочник умеет сейчас: хранение (`config/accounts`, зеркало в `localStorage`, подписка),
карточка счёта, массовое заведение подсказками с подтверждением человеком, экран инвентаризации
(поиск, фильтр по виду, пакетные действия, авторство правок), KPI «Счетов» по подтверждённым
записям и - шагом 6 - **признак ФНС счёта берётся из справочника** (`_accFnsOf`).

Ни одна задача не брошена посередине: каждая правка проверена на стенде и закоммичена отдельно.
Незакрытыми остались вопросы, требующие решения Олега, и одна задача с кодом (список ниже).

## Принципы проекта (нарушать нельзя)

1. **Один файл.** `index.html` - весь HTML/CSS/JS. Без фреймворков и сборщиков, хостинг GitHub Pages.
2. **Изоляция вкладок.** Работая с одной вкладкой, не трогать код других. У каждой свои фильтры
   (`_tabFilters[tab]`), свои «защищённые» функции.
3. **Данные read-only.** Дашборд ничего не пишет в Sheets. Правки данных - только в источнике.
   Пишется только `config/*` в Firebase RTDB, и только через `_fbSetSafe`.
4. **Числа не сокращать.** Никаких «415К» и «1,25М» в отчётах и подсказках. Сокращения допустимы
   только на подписях осей графиков и надписях на холсте.
5. **Дефис, не тире.** Нигде не использовать «—», только «-» (в коде, в UI и в переписке).
6. **Автопуш** после каждого коммита, без вопросов.
7. **Цвет = знак значения.** Доход зелёный, расход красный, остаток оранжевый. Светофор - только
   там, где у показателя есть норма.

## Открытые задачи

### Ждёт кода (решение принято, нужно сделать)

| Задача | Состояние | Что именно осталось |
|---|---|---|
| Аномалии: перемещение с заполненными обеими колонками | не начато | Проверено по коду 03.08: правил про это по-прежнему два - `move` (только `Фин. операция = Техническая операция`) и `movebal` (парность по колонке Поступление, `group:true`). Строка типа «Перемещение», где заполнено и Поступление, и Списание, не ловится ничем. |

### Ждёт решения Олега

| Вопрос | Что известно | Варианты |
|---|---|---|
| **Проверка облачного сохранения справочника** | Правило записи `config/accounts` заведено 03.08 (коммит `f4e8081`), но **сохранением из браузера ещё не проверено**: у меня стенд работает на `localStorage` и облако не трогает вовсе. До проверки неизвестно, доезжает ли справочник до второго устройства. | Открыть карточку любого счёта, сменить банк, сохранить - и убедиться, что правка появилась в RTDB и на другом устройстве. Делает Олег: реквизитов и доступа к консоли у меня нет. |
| **Канал счёта по ВИДУ из справочника** | Вид счёта теперь ХРАНИТСЯ (справочник), но `bankAccChannel` по-прежнему угадывает канал по имени + `BANK_ACC_CHANNEL_FIX`. Проверено по коду 03.08: перевода на вид нет. Три вида молча падают в «прочие расчёты»: **Карта АУ, Корпоративные карты, Топливные карты** - regex `/карт[а-яё]*\s*фл/` их не матчит. | Решение отложено 03.08 сознательно: куда отнести эти три вида - вопрос учёта, а не кода. (1) все три → «карты физлиц»; (2) Карта АУ → карты, корпоративная и топливные → прочие расчёты; (3) оставить как есть (тогда меняется только то, что сторож перестанет кричать). |
| Тумблер «Будущие оплаты» | Замер на боевой базе 02.08.2026: **3 строки, 30 000 ₽ списаний, 0,01%**; поступлений будущих нет. Механизм искажения проверен на стенде: при включённом тумблере в текущее окно попадает план, а в прошлогоднее - только факт, дельта завышается (на стенде +56% против +25%). Сальдо и Runway тоже двигаются. | Сейчас не трогаем - при 0,01% срочности нет. **Условие возврата: появление регулярных будущих оплат** (зарплата, налоги, аренда, проставленные вперёд). Тогда: либо исключить будущее из запаса всегда, либо выключить тумблер по умолчанию. |
| Колонка «Вид счёта» в источнике | ⚠ Роль изменилась: как ХРАНИЛИЩЕ вида колонка больше не нужна - вид держит справочник, и переименование счёта в источнике его уже не меняет. Осталась одна причина: справочник ведут руками, а колонка заполнялась бы при заведении операции. | Низкий приоритет. Работа в источнике, не в коде. |
| Пять подстатей без ответственного | Проверяется только по живым данным - в коде этой величины нет, есть лишь `NOOWN='(не назначен)'` в своде ЦФО. | Открыть БДР → «Исполнение по ЦФО», строка «(не назначен)»; либо назначить владельцев в редакторе планов, либо признать, что эти подстатьи ничьи сознательно. |
| Четыре пункта регламента БДР | **Исходного текста нет** - ни в коде, ни в `CLAUDE.md`, ни в моей памяти проекта. `handoff.md` в репозитории отсутствовал (проверено по `git log --all`), так что восстановить формулировки неоткуда. | Прислать список - разберу каждый пункт. |

### Закрыто в этой сессии (проверено по коду 03.08)

- **Справочник счетов, шаги 1-6** - `config/accounts`, карточка, массовое заведение, инвентаризация,
  KPI «Счетов», признак ФНС из справочника. Правило записи ветки заведено (`f4e8081`).
- **Вопрос «Счетов на вкладке БАНК»** - снят шагом 5: показатель считает подтверждённые записи
  справочника (`ok===1`), от фильтров не зависит вовсе, подпись «в справочнике», закрытые названы
  отдельно («5 · 2 закрыты»). Пока справочник пуст - показывает прежнее число счетов среза и честно
  пишет «справочник не заполнен» (ноль в этом месте читался бы как поломка). Из трёх прежних
  вариантов не выбран ни один - величина стала инвентарной, а не производной от среза.
- **`_mktCounts` → `config/mktCounts`** - сделано 02.08 (`_mktSaveCounts` пишет через `_fbSetSafe`,
  подписка `startMktCountsSync`). В прошлом handoff числилось «не начато» - строка была устаревшей.
- **Сторож уровней плана ПРИ ОТКРЫТИИ редактора** - сделано (`_planAlienHtml`, третий слой к гейту
  записи и фильтру заморозки). Разбивка по режиму, не одним числом: в наборе вперемешку рубли
  и проценты. Удаление не предлагает - только сообщает.
- **Чистка зеркал `localStorage` при выходе** - сделано (`LS_MIRROR_KEYS` + `_lsWipeMirrors`
  в `appLogout`, 17 ключей, включая `finAccounts`). ⚠ `fin_rawHeaders_*` не чистится сознательно -
  это эталон шапки листа, после чистки сторож перестановки колонок один раз промолчал бы.
- **`console.warn` в пустых `catch` у `setItem`** - сделано: `_lsWarn(key,e,op)` (index.html:1204),
  32 вызова по файлу, отдельная формулировка для записи и для удаления зеркала. ⚠ Пустых `catch(e){}`
  в файле по-прежнему 84 - правка закрыла только `localStorage`, остальные не трогались.

### Закрыто в прошлых сессиях (не поднимать заново)

- **Чистка `config/plansBaseline` от фантома 74 666 932 ₽ и 47 записей `level='article'`** - проведена
  31.07.2026, обе ветки, метод контроля `__pfSnap` (4792 числа в 26 разрезах совпали поэлементно).
  В коде стоят два гейта против повтора: `_planStage` не пишет чужие уровни, `_baselineBakeMonth`
  не запекает их в baseline. ⚠ Проверить фактическое состояние облака можно только снимком - код
  этого не показывает.
- **Источник количеств лидов/сделок по направлениям** - закрыт 31.07.2026: направление сделки
  без отдельного правила разнесения не определяется (30% договоров проходят сразу по двум
  направлениям). Не искать заново, причины в `CLAUDE.md`.
- **Бэклог ревизии 2026-07** - закрыт. Осталось только сознательно пропущенное: дедупликация
  `esc`/`ds`/print-CSS (не трогаем, это security-код с шестью разными поведениями).

## Ловушки (первые три - этой сессии, остальные - прошлой)

Полные формулировки - в `CLAUDE.md` (разделы «Ловушки по вкладкам» и «Общие правила»).
Здесь - список, чтобы знать, что искать.

- **Признак ФНС счёта: источников ДВА, порядок фиксирован** (`_accFnsOf`). Подтверждённая запись
  справочника (`ok===1`) перебивает факт из строк; `ok:0` не перебивает - её признак предзаполнен
  той же машинной подсказкой из тех же строк. Правило одно на весь Банк (свод, фильтр «ФНС»,
  «Доля оформленного»), второй копии нет.
- **Подмена признака обязана быть ЗАМЕТНА.** Три следа: пометка «спр.» перед именем счёта в своде,
  счётчик расхождений в инвентаризации, ⚠ в колонке ФНС. Без них счёт молча переезжает между
  «Налоговой выручкой» и «ДС до налогового оформления», и сверка с источником расходится
  необъяснимо. ⚠ Пометка ПЕРЕД именем, а не после: `td.bank-name` обрезает многоточием.
- **Счётчик расхождений считает ПРОТИВОРЕЧИЯ**, а не всякую подмену: «в справочнике да, в строках
  признака нет» - дополнение к неполным данным, в счётчик не идёт (а пометка «спр.» в своде
  у такого счёта стоит - раскладку он всё-таки меняет).
  ⚠ Мемо-карта признака (`_fnsOfAcc`) живёт ВНУТРИ `renderBank` и пересобирается на каждый вызов -
  правку справочника она пережить не может по построению. Проверено тремя перерисовками подряд
  с разным `_accounts` в одной странице: счёт переезжает между карточками и пометка появляется
  и исчезает как надо. Заводить такой кэш СНАРУЖИ `renderBank` нельзя - вот там он залипнет.
- **Поиск Реестра: у текста и чисел РАЗНЫЕ правила, и это не непоследовательность.** Текст -
  префикс («иван» находит «Иванову»), число - целое слово («15 000,00» не находит 150 000).
  Служебные слова в одну-две буквы в отбор не идут. Запрос целиком из коротких токенов ищется
  как отдельные слова. Регулярки только с флагом `u`.
- **Банк: поток и запас - величины разной природы.** Период применяется только к потоку.
  Запас (Сальдо, Cash Runway, лимиты, колонка «Итого») считается от начала учёта до ВЕРХНЕЙ
  границы периода; нижняя на него не влияет. Контрольное правило: Σ «Итого» по трём сводам
  = Сальдо, при любом периоде и под фильтром ФНС.
- **Правки внутри `<style>` - считать `cssRules.length` до и после.** Ориентир на 03.08.2026: **346**
  (перемерено на стенде, за сессию не изменилось - в стиль не лезли). ⚠ Реальный `<style>` в документе
  ОДИН: текстовый поиск даёт 15 совпадений, но 14 из них лежат внутри JS-шаблонов печати.
  Балансировка скобок такие поломки не ловит: 01.08 вырезанный диапазон снёс ~120 правил при
  идеально сошедшихся скобках.
- **Ширины колонок мерить по `scrollWidth` отрисованной ячейки, не `canvas.measureText`.** Канвас
  не применяет `tabular-nums` и занижает на 6-14px. Ошибка повторилась пять раз подряд.
- **Снимок для консоли обязан начинаться с преамбулы**: переход на вкладку, ожидание её узла,
  `typeof RAW==='undefined'` вместо `!window.RAW` (`RAW` объявлена через `let`, на `window` её нет).
  Рендереры выходят первой строкой без своего контейнера - снимок с чужой вкладки отдаёт нули,
  похожие на дефект кода. За сессию это случилось трижды.
- **Замер геометрии в headless шумит на ±4px** от прогона к прогону (похоже, подгрузка шрифтов) -
  числа брать по повторяющимся значениям, каждое подтверждать двумя прогонами.

## Данные

- Google Sheets API, лист `RawData`, 15 колонок, ~61 тыс. строк. Даты приходят Excel-серийными
  числами. Индексы колонок фиксированы (`COL`) - перестановка молча ломает отчёты.
- Открытие ~3,6 с после заливки значениями (`apps-script/freeze-rawdata.gs`). Время растёт
  ЛИНЕЙНО по числу строк. Возвращаться к разговору о загрузке по периоду - когда перевалит
  за 8-10 секунд, не раньше.
- **Кэша RAW между открытиями нет и пока не будет** - 23,5 МБ чужих финансовых данных на диске
  не стоят экономии трёх секунд.
- Реквизиты подключения (`sheetId`/`apiKey`) лежат у Олега в `localStorage` и `config/sheets`,
  в репозитории их нет. **Замеры на боевой базе снимает только Олег** - я могу дать снимок
  для консоли, но выполнить его сам не могу.
- Контрольная сверка после любой правки расчётов БДР: «ИТОГО РАСХОДЫ факт» в БДР = «РАСХОДЫ»
  в ОПиУ за тот же период и направление (без вывода 127). Сверяется РАВЕНСТВО отчётов,
  а не совпадение с фиксированным числом.

## Рабочие правила

- **Порядок:** `git status` → правка → проверка на `python3 -m http.server 8000` → `git status`
  → один коммит. Коммитить после каждого шага, не пакетом.
- **Диагностика до правки.** Если задача звучит как «почини» - сначала доложить, что найдено,
  и дождаться «ок». Половина пунктов сессии 02.08 оказалась уже сделанной или основанной
  на устаревшем замере. ⚠ Повторилось 03.08 при сверке этого файла: из пяти задач в разделе
  «ждёт кода» три были давно закрыты в коде. **Сверять по коду, а не по прошлому handoff** -
  сам handoff устаревает первым.
- **Локальная проверка обязательна.** `file://` не годится - Firebase Auth работает только
  по http/https. Поднимать `python3 -m http.server 8000`, открывать `http://localhost:8000`.
- **Проверять на стенде headless-Chrome.** Стенд = копия `index.html` с вырезанными CDN-скриптами
  (Chart.js подменяется заглушкой, Firebase отсутствует - работает fail-open, вход не нужен),
  заглушкой `fetch` на синтетический `RawData` и реквизитами в `localStorage`. Стенд живёт
  в scratchpad, в репозиторий его не коммитить.
  - ⚠ **Гонять через CDP** (`--remote-debugging-port` + `WebSocket` из Node 22+, без зависимостей),
    а НЕ через `--dump-dom --virtual-time-budget`: виртуальное время сгорает на таймерах страницы
    раньше, чем доедет проверка, и дамп приходит пустым - выглядит как «проверка не отработала».
  - ⚠ **Любая вставка `<script>` - только по ПОСЛЕДНЕМУ `</body>`.** Первое вхождение лежит внутри
    шаблонной строки печати (`index.html:1769`), и вставка туда рвёт главный скрипт пополам:
    приложение молча теряет `switchTab` и всё остальное. Ошибка сделана 03.08 - опознаётся
    по `SyntaxError: Unexpected token '<'` в консоли.
  - ⚠ **Порт 8000 может быть занят** сервером Олега, отдающим сам проект. Проверять
    (`lsof -ti tcp:8000`), чужой процесс не убивать, стенд поднимать рядом (8010).
- **Числа в отчёте называть с датасетом.** «На стенде (47 синтетических строк)» и «на боевой базе»
  - разные вещи, и путать их нельзя.
- **Не сокращать проверку до утверждения.** Если факт можно измерить - измерить, а не вывести
  из кода. За сессию два вывода «по логике» оказались неверными.
